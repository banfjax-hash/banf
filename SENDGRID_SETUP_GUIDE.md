# ≡ƒöÉ SENDGRID API KEY SETUP INSTRUCTIONS**Date:** February 20, 2026  **API Key Name:** banf_api_key  **Status:** Ready to configure---## ≡ƒôï API KEY DETAILS```Secret Name (in Wix):  banf_api_keySecret Value:          SG.cHuZWMNWTuaU5-zzokG9ng.ez2re9zMosZ_lcebVa_80Ay7Tl5hQ_4dpHt27UpC3JUService:               SendGrid Email APIPermissions:           Full Access (Mail Send)```---## ≡ƒÄ» HOW TO CONFIGURE IN WIX DASHBOARD### Step 1: Log in to Wix1. Open: https://www.wix.com/dashboard2. Log in as: **banfjax@gmail.com**### Step 2: Navigate to Secrets Manager1. Click: **Settings** (bottom left sidebar)2. Select: **Secrets Manager**### Step 3: Add the Secret1. Click: **+ Add Secret** button2. Enter the following:**Secret Name:**```banf_api_key```**Secret Value:**```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```3. Click: **Save**### Step 4: Verify ConfigurationOnce saved, you should see:- Γ£à Secret name: `banf_api_key`- Γ£à Status: Active- Γ£à Value: (hidden, showing SG.xxxx...)---## ≡ƒöä UPDATE BACKEND CODE (IF NEEDED)The current code looks for a secret named `banf`, but you'll be using `banf_api_key`.If you want to update the backend code to use this new secret name:**File:** `src/backend/http-functions.js` (line 642)Change from:```javascript_sendgridKey = await getSecret('banf');```To:```javascript_sendgridKey = await getSecret('banf_api_key');```---## ≡ƒº¬ TEST EMAIL AFTER CONFIGURATIONAfter setting the secret in Wix dashboard:1. **Wait 2-3 minutes** for Wix to recognize the new secret2. **Run the email test:**   ```powershell   cd c:\projects\banf   node test-email-delivery.js   ```3. **Check inbox:** ranadhir.ghosh@gmail.com (should receive test email within 1-2 minutes)---## Γ£à SUCCESS INDICATORSOnce configured, you should see:Γ£à **Email Status Endpoint Returns:**```json{  "status": "healthy",  "provider": "sendgrid",  "configured": true}```Γ£à **Test Email Sends Successfully:**```Status: 250 (OK - Email accepted by SendGrid)Message ID generatedTimestamp recorded```Γ£à **Email Received:**```Recipient: ranadhir.ghosh@gmail.comFrom: BANF <banfjax@gmail.com>Subject: ≡ƒº¬ BANF Email System TestDelivery: 1-2 minutes```---## ≡ƒöì API KEY VALIDATIONYour SendGrid API Key Details:- **Format:** Γ£à Valid (starts with `SG.`)- **Permissions:** Full access for email sending- **Status:** Active and ready- **Expiration:** None (permanent unless revoked)---## ≡ƒÜÇ NEXT STEPS1. Add `banf_api_key` secret to Wix Secrets Manager (THIS PAGE)2. Wait 2-3 minutes for Wix to cache the secret3. Run email delivery test4. Verify email received at ranadhir.ghosh@gmail.com5. All workflows automatically enabled! Γ£à---## ≡ƒô₧ TROUBLESHOOTING### Email still not sending?1. Verify the exact SpellCheck:   - Secret name: `banf_api_key` (exact case)   - Value: starts with `SG.`2. Wait 5 minutes and try again3. Check Wix dashboard ΓåÆ Secrets Manager shows the secret is "Active"### Can't find Secrets Manager?1. Go to: https://www.wix.com/dashboard2. Bottom left: Click ΓÜÖ∩╕Å **Settings**3. Look for: **Secrets Manager** in the settings menu4. If not visible, your plan may need upgrade### API key looks wrong?- Do NOT share this key in public channels- The key starts with: `SG.`- Length should be: ~90 characters---**Setup Complete When:**- Γ£à Secret added to Wix Secrets Manager- Γ£à Status shows "Active"- Γ£à Email test sends successfully**Status:** Ready for configuration ≡ƒÄ»