#!/usr/bin/env node/** * ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ *  BANF Event Manager Agent ΓÇö Drive Lifecycle Controller * ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ * *  Reads banf-drives.yaml and manages the lifecycle state of all pipeline drives. *  The email reader agent consults this module before routing emails to pipelines. * *  Responsibilities: *    1. Parse YAML drive configuration *    2. Maintain drive-state.json with current status of each drive *    3. Auto-activate EVENT_BOUND drives when trigger conditions are met *    4. Auto-deactivate EVENT_BOUND drives when event date has passed *    5. Support manual activation/deactivation of TRIGGERED drives *    6. Provide isDriveActive(driveId) API for email reader *    7. Provide getActiveDrives() for dashboard/reporting * *  CLI: *    node banf-event-manager.js --status              # Show all drive statuses *    node banf-event-manager.js --activate <drive>    # Manually activate a drive *    node banf-event-manager.js --deactivate <drive>  # Manually deactivate a drive *    node banf-event-manager.js --sync                # Sync state with YAML + auto-lifecycle *    node banf-event-manager.js --events              # Show all events and their status *    node banf-event-manager.js --bind <drive> <event># Bind a drive to an event * * ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ */const fs = require('fs');const path = require('path');// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// YAML PARSER (lightweight, no external deps)// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ/** * Minimal YAML parser for the drive config format. * Handles: scalars, lists (- item), nested objects, comments (#), multi-line (>). * Does NOT handle anchors/aliases, complex types, or flow notation. */function parseYAML(text) {  const lines = text.split('\n');  const root = {};  const stack = [{ obj: root, indent: -1 }];  for (let i = 0; i < lines.length; i++) {    let line = lines[i];    // Strip comments (but not inside quoted strings)    const commentIdx = findCommentStart(line);    if (commentIdx >= 0) line = line.substring(0, commentIdx);    // Skip empty lines    if (line.trim() === '') continue;    const indent = line.search(/\S/);    const content = line.trim();    // List item (- value)    if (content.startsWith('- ')) {      const value = content.substring(2).trim();      // Walk up the stack to find the array parent      while (stack.length > 1 && stack[stack.length - 1].indent >= indent) {        stack.pop();      }      const parent = stack[stack.length - 1];      // The parent's last key should be an array      const lastKey = getLastKey(parent.obj);      if (lastKey != null && Array.isArray(parent.obj[lastKey])) {        parent.obj[lastKey].push(parseScalar(value));      }      continue;    }    // Key: Value pair    const colonIdx = content.indexOf(':');    if (colonIdx > 0) {      const key = content.substring(0, colonIdx).trim();      let value = content.substring(colonIdx + 1).trim();      const parent = findParent(stack, indent);      if (value === '' || value === '>') {        // Could be nested object OR array ΓÇö peek ahead        if (value === '>') {          // Multi-line scalar ΓÇö collect until indent decreases          let multiLine = '';          while (i + 1 < lines.length) {            const nextLine = lines[i + 1];            const nextIndent = nextLine.search(/\S/);            if (nextIndent <= indent && nextLine.trim() !== '') break;            if (nextLine.trim() === '') { multiLine += '\n'; i++; continue; }            multiLine += (multiLine ? ' ' : '') + nextLine.trim();            i++;          }          parent.obj[key] = multiLine;        } else {          // Peek ahead to see if next non-empty line is a list item          let nextIdx = i + 1;          while (nextIdx < lines.length && lines[nextIdx].trim() === '') nextIdx++;          if (nextIdx < lines.length && lines[nextIdx].trim().startsWith('- ')) {            parent.obj[key] = [];  // Initialize as array ΓÇö DON'T push to stack          } else {            parent.obj[key] = {};            stack.push({ obj: parent.obj[key], indent, key });          }        }      } else {        parent.obj[key] = parseScalar(value);      }    }  }  return root;}function findCommentStart(line) {  let inSingle = false, inDouble = false;  for (let i = 0; i < line.length; i++) {    if (line[i] === "'" && !inDouble) inSingle = !inSingle;    if (line[i] === '"' && !inSingle) inDouble = !inDouble;    if (line[i] === '#' && !inSingle && !inDouble && (i === 0 || line[i - 1] === ' ')) return i;  }  return -1;}function findParent(stack, indent) {  while (stack.length > 1 && stack[stack.length - 1].indent >= indent) {    stack.pop();  }  return stack[stack.length - 1];}function getLastKey(obj) {  const keys = Object.keys(obj);  return keys.length > 0 ? keys[keys.length - 1] : null;}function parseScalar(value) {  if (value === 'null' || value === '~') return null;  if (value === 'true') return true;  if (value === 'false') return false;  if (/^-?\d+$/.test(value)) return parseInt(value, 10);  if (/^-?\d+\.\d+$/.test(value)) return parseFloat(value);  // Strip quotes  if ((value.startsWith('"') && value.endsWith('"')) ||      (value.startsWith("'") && value.endsWith("'"))) {    return value.slice(1, -1);  }  return value;}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// CONFIGURATION// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇconst YAML_FILE = path.join(__dirname, 'banf-drives.yaml');const STATE_FILE = path.join(__dirname, 'drive-state.json');const LOG_FILE = path.join(__dirname, 'drive-manager.log');function log(level, msg) {  const ts = new Date().toISOString();  const line = `[${ts}] [${level}] [EventManager] ${msg}`;  console.log(line);  try {    fs.appendFileSync(LOG_FILE, line + '\n');  } catch { /* ignore */ }}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// STATE MANAGEMENT// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇfunction loadYAMLConfig() {  try {    const raw = fs.readFileSync(YAML_FILE, 'utf8');    return parseYAML(raw);  } catch (e) {    log('ERROR', `Failed to load YAML config: ${e.message}`);    return { drives: {}, events: {}, global: {} };  }}function loadState() {  try {    if (fs.existsSync(STATE_FILE)) {      return JSON.parse(fs.readFileSync(STATE_FILE, 'utf8'));    }  } catch (e) {    log('WARN', `Failed to load state: ${e.message}`);  }  return {    driveStates: {},    // driveId -> { status, activatedAt, deactivatedAt, boundEvent, ... }    eventStates: {},    // eventId -> { status, driveStatuses }    lastSyncAt: null,    syncCount: 0,    createdAt: new Date().toISOString()  };}function saveState(state) {  state.lastSaved = new Date().toISOString();  fs.writeFileSync(STATE_FILE, JSON.stringify(state, null, 2));}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// DRIVE LIFECYCLE ENGINE// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ/** * Sync drive state with YAML config. * - Registers new drives from YAML * - Auto-activates ALWAYS_ON drives * - Auto-transitions EVENT_BOUND drives based on date * - Preserves manual TRIGGERED drive states */function syncDriveState() {  const config = loadYAMLConfig();  const state = loadState();  const now = new Date();  const today = now.toISOString().split('T')[0]; // YYYY-MM-DD  const drives = config.drives || {};  const events = config.events || {};  let activated = 0, deactivated = 0, unchanged = 0;  for (const [driveId, driveDef] of Object.entries(drives)) {    const existing = state.driveStates[driveId] || {};    const driveType = driveDef.type;    // Initialize if new    if (!state.driveStates[driveId]) {      state.driveStates[driveId] = {        driveId,        name: driveDef.name,        type: driveType,        status: 'dormant',        activatedAt: null,        deactivatedAt: null,        boundEvent: null,        phase: driveDef.phase,        handler: driveDef.handler,        lastCheckedAt: now.toISOString()      };    }    const driveState = state.driveStates[driveId];    driveState.lastCheckedAt = now.toISOString();    driveState.name = driveDef.name;    driveState.phase = driveDef.phase;    driveState.handler = driveDef.handler;    // ΓöÇΓöÇ ALWAYS_ON: force active ΓöÇΓöÇ    if (driveType === 'ALWAYS_ON') {      if (driveState.status !== 'active') {        driveState.status = 'active';        driveState.activatedAt = driveState.activatedAt || now.toISOString();        log('INFO', `  Γ£à ${driveId} ΓåÆ ACTIVE (always-on)`);        activated++;      } else {        unchanged++;      }      continue;    }    // ΓöÇΓöÇ EVENT_BOUND: check event dates ΓöÇΓöÇ    if (driveType === 'EVENT_BOUND') {      // Find the bound event      const boundEventId = driveState.boundEvent;      let eventDef = boundEventId ? events[boundEventId] : null;      // If no event is bound, try to find the next upcoming event that uses this drive      if (!eventDef) {        for (const [evId, evDef] of Object.entries(events)) {          const evDrives = evDef.drives || [];          if (evDrives.includes(driveId) && evDef.status !== 'completed') {            eventDef = evDef;            driveState.boundEvent = evId;            break;          }        }      }      if (eventDef) {        const eventDate = eventDef.date;        if (eventDate && eventDate < today) {          // Event has passed ΓåÆ complete the drive          if (driveState.status === 'active') {            driveState.status = 'completed';            driveState.deactivatedAt = now.toISOString();            driveState.completionReason = 'event_date_passed';            log('INFO', `  ΓÅ╣∩╕Å  ${driveId} ΓåÆ COMPLETED (event ${driveState.boundEvent} on ${eventDate} has passed)`);            deactivated++;          } else if (driveState.status !== 'completed') {            driveState.status = 'completed';            unchanged++;          }        } else if (eventDate && eventDate >= today) {          // Event is upcoming or today          // For drives with trigger: first_rsvp_email, stay dormant until first email          // For trigger: event_day, activate on event day only          const trigger = driveDef.activation?.trigger;          if (trigger === 'event_day' && eventDate === today) {            if (driveState.status !== 'active') {              driveState.status = 'active';              driveState.activatedAt = now.toISOString();              log('INFO', `  Γ£à ${driveId} ΓåÆ ACTIVE (event day: ${eventDate})`);              activated++;            }          } else if (trigger === 'event_completed') {            // Post-event drive ΓÇö activate day after event            const dayAfter = new Date(eventDate);            dayAfter.setDate(dayAfter.getDate() + 1);            const dayAfterStr = dayAfter.toISOString().split('T')[0];            if (today >= dayAfterStr && driveState.status !== 'active' && driveState.status !== 'completed') {              driveState.status = 'active';              driveState.activatedAt = now.toISOString();              log('INFO', `  Γ£à ${driveId} ΓåÆ ACTIVE (post-event, day after ${eventDate})`);              activated++;            }          }          // first_rsvp_email trigger is handled externally when first Evite email arrives        }      } else {        // No event bound ΓÇö stay dormant        if (driveState.status !== 'dormant') {          unchanged++;        }      }      continue;    }    // ΓöÇΓöÇ TRIGGERED: preserve manual state ΓöÇΓöÇ    if (driveType === 'TRIGGERED') {      // Don't auto-change state ΓÇö only manual activate/deactivate      unchanged++;      continue;    }  }  // Sync event states  for (const [eventId, eventDef] of Object.entries(events)) {    const eventDate = eventDef.date;    let eventStatus = eventDef.status || 'dormant';    if (eventDate && eventDate < today && eventStatus !== 'completed') {      eventStatus = 'completed';    }    state.eventStates[eventId] = {      eventId,      name: eventDef.name,      date: eventDate,      type: eventDef.type,      status: eventStatus,      drives: eventDef.drives || [],      stats: eventDef.stats || {},      lastCheckedAt: now.toISOString()    };  }  state.lastSyncAt = now.toISOString();  state.syncCount = (state.syncCount || 0) + 1;  saveState(state);  log('INFO', `Sync complete: ${activated} activated, ${deactivated} deactivated, ${unchanged} unchanged`);  return { activated, deactivated, unchanged, totalDrives: Object.keys(drives).length };}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// PUBLIC APIs (for email reader integration)// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ/** * Check if a specific drive is currently active. * @param {string} driveId - Drive identifier from YAML (e.g., 'evite_rsvp') * @returns {boolean} */function isDriveActive(driveId) {  try {    const state = loadState();    const driveState = state.driveStates[driveId];    if (!driveState) {      // Drive not in state ΓÇö check YAML for ALWAYS_ON      const config = loadYAMLConfig();      const driveDef = config.drives?.[driveId];      return driveDef?.type === 'ALWAYS_ON';    }    return driveState.status === 'active';  } catch {    return false;  }}/** * Get all currently active drives. * @returns {Array} [{ driveId, name, type, phase, status, boundEvent }] */function getActiveDrives() {  try {    const state = loadState();    return Object.values(state.driveStates)      .filter(d => d.status === 'active')      .map(d => ({        driveId: d.driveId,        name: d.name,        type: d.type,        phase: d.phase,        status: d.status,        boundEvent: d.boundEvent,        activatedAt: d.activatedAt      }));  } catch {    return [];  }}/** * Get the full drive state for a specific drive. * @param {string} driveId * @returns {object|null} */function getDriveState(driveId) {  try {    const state = loadState();    return state.driveStates[driveId] || null;  } catch {    return null;  }}/** * Get all events and their states. */function getEvents() {  try {    const state = loadState();    return Object.values(state.eventStates);  } catch {    return [];  }}/** * Manually activate a drive. * @param {string} driveId * @param {string} reason - Why the drive is being activated * @returns {object} { success, driveId, previousStatus, newStatus } */function activateDrive(driveId, reason) {  const state = loadState();  if (!state.driveStates[driveId]) {    // Initialize from YAML    const config = loadYAMLConfig();    const driveDef = config.drives?.[driveId];    if (!driveDef) return { success: false, error: `Drive '${driveId}' not found in YAML config` };    state.driveStates[driveId] = {      driveId,      name: driveDef.name,      type: driveDef.type,      status: 'dormant',      phase: driveDef.phase,      handler: driveDef.handler    };  }  const driveState = state.driveStates[driveId];  const previousStatus = driveState.status;  driveState.status = 'active';  driveState.activatedAt = new Date().toISOString();  driveState.activationReason = reason || 'manual';  driveState.deactivatedAt = null;  saveState(state);  log('INFO', `Γ£à Drive '${driveId}' activated (was: ${previousStatus}). Reason: ${reason || 'manual'}`);  return { success: true, driveId, previousStatus, newStatus: 'active' };}/** * Manually deactivate a drive. * @param {string} driveId * @param {string} reason */function deactivateDrive(driveId, reason) {  const state = loadState();  const driveState = state.driveStates[driveId];  if (!driveState) return { success: false, error: `Drive '${driveId}' not in state` };  const previousStatus = driveState.status;  driveState.status = 'dormant';  driveState.deactivatedAt = new Date().toISOString();  driveState.deactivationReason = reason || 'manual';  saveState(state);  log('INFO', `ΓÅ╣∩╕Å  Drive '${driveId}' deactivated (was: ${previousStatus}). Reason: ${reason || 'manual'}`);  return { success: true, driveId, previousStatus, newStatus: 'dormant' };}/** * Notify the event manager that the first RSVP email for an event has arrived. * This auto-activates the evite_rsvp drive for that event. */function notifyFirstRsvp(eventId) {  const state = loadState();  const config = loadYAMLConfig();  // Find the evite_rsvp drive and bind it to this event  const rsvpDrive = state.driveStates['evite_rsvp'];  if (rsvpDrive && rsvpDrive.status !== 'active') {    rsvpDrive.status = 'active';    rsvpDrive.activatedAt = new Date().toISOString();    rsvpDrive.boundEvent = eventId;    rsvpDrive.activationReason = 'first_rsvp_email';    // Also set the event date as drive end date    const eventDef = config.events?.[eventId];    if (eventDef) {      rsvpDrive.eventDate = eventDef.date;    }    saveState(state);    log('INFO', `Γ£à evite_rsvp activated by first RSVP for event: ${eventId}`);    return { success: true, driveId: 'evite_rsvp', event: eventId };  }  return { success: false, reason: 'already_active_or_missing' };}/** * Bind a drive to a specific event. */function bindDriveToEvent(driveId, eventId) {  const state = loadState();  const driveState = state.driveStates[driveId];  if (!driveState) return { success: false, error: `Drive '${driveId}' not in state` };  driveState.boundEvent = eventId;  saveState(state);  log('INFO', `≡ƒöù Drive '${driveId}' bound to event '${eventId}'`);  return { success: true, driveId, eventId };}/** * Get the YAML configuration for a drive. */function getDriveConfig(driveId) {  const config = loadYAMLConfig();  return config.drives?.[driveId] || null;}/** * Get all drive definitions from YAML. */function getAllDriveConfigs() {  const config = loadYAMLConfig();  return config.drives || {};}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// MODULE EXPORTS// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇmodule.exports = {  // Core APIs  isDriveActive,  getActiveDrives,  getDriveState,  getDriveConfig,  getAllDriveConfigs,  getEvents,  // Lifecycle management  syncDriveState,  activateDrive,  deactivateDrive,  bindDriveToEvent,  notifyFirstRsvp,  // Utilities  loadYAMLConfig,  loadState,  parseYAML};// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// CLI// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇif (require.main === module) {  const args = process.argv.slice(2);  if (args.includes('--status')) {    // Run sync first    syncDriveState();    const state = loadState();    console.log('\nΓòöΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòù');    console.log('Γòæ  BANF Event Manager ΓÇö Drive Status                           Γòæ');    console.log('ΓòÜΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓò¥');    const byType = { ALWAYS_ON: [], EVENT_BOUND: [], TRIGGERED: [] };    for (const [id, d] of Object.entries(state.driveStates)) {      const type = d.type || 'UNKNOWN';      if (!byType[type]) byType[type] = [];      byType[type].push({ id, ...d });    }    for (const [type, drives] of Object.entries(byType)) {      if (drives.length === 0) continue;      console.log(`\n  ΓöÇΓöÇ ${type} ΓöÇΓöÇ`);      for (const d of drives) {        const icon = d.status === 'active' ? '≡ƒƒó' : d.status === 'completed' ? 'Γ£à' : d.status === 'suspended' ? 'ΓÅ╕∩╕Å' : 'ΓÜ½';        const event = d.boundEvent ? ` [${d.boundEvent}]` : '';        const phase = d.phase ? ` (Phase ${d.phase})` : '';        console.log(`    ${icon} ${d.id}: ${d.status.toUpperCase()}${phase}${event}`);        if (d.activatedAt) console.log(`       Activated: ${d.activatedAt}`);      }    }    const active = Object.values(state.driveStates).filter(d => d.status === 'active').length;    const total = Object.keys(state.driveStates).length;    console.log(`\n  Total: ${total} drives, ${active} active`);    console.log(`  Last sync: ${state.lastSyncAt}`);  }  else if (args.includes('--events')) {    syncDriveState();    const state = loadState();    console.log('\nΓòöΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòù');    console.log('Γòæ  BANF Event Manager ΓÇö Events                                 Γòæ');    console.log('ΓòÜΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓò¥');    for (const [id, ev] of Object.entries(state.eventStates)) {      const icon = ev.status === 'completed' ? 'Γ£à' : ev.status === 'active' ? '≡ƒƒó' : '≡ƒôà';      console.log(`\n  ${icon} ${ev.name} (${ev.date})`);      console.log(`     Status: ${ev.status} | Type: ${ev.type}`);      if (ev.drives?.length > 0) console.log(`     Drives: ${ev.drives.join(', ')}`);      if (ev.stats && Object.keys(ev.stats).length > 0) {        console.log(`     Stats: ${JSON.stringify(ev.stats)}`);      }    }  }  else if (args.includes('--activate')) {    const driveId = args[args.indexOf('--activate') + 1];    if (!driveId) { console.log('Usage: --activate <drive_id>'); process.exit(1); }    const result = activateDrive(driveId, 'cli_manual');    console.log(result.success ? `Γ£à ${driveId} activated` : `Γ¥î ${result.error}`);  }  else if (args.includes('--deactivate')) {    const driveId = args[args.indexOf('--deactivate') + 1];    if (!driveId) { console.log('Usage: --deactivate <drive_id>'); process.exit(1); }    const result = deactivateDrive(driveId, 'cli_manual');    console.log(result.success ? `ΓÅ╣∩╕Å  ${driveId} deactivated` : `Γ¥î ${result.error}`);  }  else if (args.includes('--sync')) {    const result = syncDriveState();    console.log(`\nΓ£à Sync complete: ${result.activated} activated, ${result.deactivated} deactivated, ${result.unchanged} unchanged (${result.totalDrives} total)`);  }  else if (args.includes('--bind')) {    const driveId = args[args.indexOf('--bind') + 1];    const eventId = args[args.indexOf('--bind') + 2];    if (!driveId || !eventId) { console.log('Usage: --bind <drive_id> <event_id>'); process.exit(1); }    const result = bindDriveToEvent(driveId, eventId);    console.log(result.success ? `≡ƒöù ${driveId} ΓåÆ ${eventId}` : `Γ¥î ${result.error}`);  }  else {    console.log('Usage: node banf-event-manager.js [options]');    console.log('  --status               Show all drive statuses');    console.log('  --events               Show all events');    console.log('  --sync                 Sync state with YAML config');    console.log('  --activate <drive>     Manually activate a drive');    console.log('  --deactivate <drive>   Deactivate a drive');    console.log('  --bind <drive> <event> Bind a drive to an event');  }}