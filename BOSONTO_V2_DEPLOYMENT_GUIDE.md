# BANF Bosonto Utsob 2026 ΓÇö Wix Server-Side Deployment Guide (v2.0)## What Changed in v2.0| Fix | Before | After ||-----|--------|-------|| **Clean Emails** | Contained agent/pipeline/stage terminology | Member-friendly only ΓÇö no technical info || **Pricing Bug** | Used halved per-person amounts (e.g. $170) | Uses `householdMembershipAmount` for correct full amounts (e.g. $340) || **Family/Spouse** | Not detected | 6-strategy detection: overrides ΓåÆ household ΓåÆ display name ΓåÆ payment name ΓåÆ family members ΓåÆ evidence || **Verify Prompt** | Missing | Every email has "ΓÜá∩╕Å Please verify" block || **Server-Side** | Local-only Node.js script | Full Wix backend with `runFullPipeline()` export |---## Files to Deploy### 1. `bosonto-workflow-service.jsw` ΓåÆ `backend/bosonto-workflow-service.jsw`**Core backend service.** Copy the entire file content to your Wix Velo backend.**Key exports:**- `runFullPipeline()` ΓÇö Runs the complete 9-stage pipeline server-side (admin only)- `getDashboardData()` ΓÇö Returns all data for the admin dashboard- `adminUpdateCRM(email, changes, reason)` ΓÇö Admin edit = benchmark of truth- `toggleTestMode(enabled)` ΓÇö Switch test mode on/off- `enableBatchProcessing()` / `disableBatchProcessing()` ΓÇö Batch processing gate- `queueBatchProcessing(emails, type)` / `approveBatch(batchId)` ΓÇö Queue and approve batches### 2. Admin Portal Page Code (existing `bosonto-admin-portal.html`)Add this to your Wix page code to connect the backend:```javascriptimport { runFullPipeline, getDashboardData, toggleTestMode } from 'backend/bosonto-workflow-service.jsw';$w.onReady(async function () {  const data = await getDashboardData();  // Render dashboard with data...});// Run pipeline button handlerexport async function runPipelineButton_click() {  const result = await runFullPipeline();  console.log(`Pipeline complete: ${result.totalMembers} members, ${result.emailsSent} emails`);}// Toggle test modeexport async function testModeToggle_change(event) {  await toggleTestMode(event.target.checked);}```---## Deployment Steps### Step 1: Open Wix Velo Editor1. Go to your Wix site editor2. Enable **Dev Mode** (Velo) if not already enabled3. Navigate to **Backend** section in the file tree### Step 2: Deploy Backend Service1. Create/replace `backend/bosonto-workflow-service.jsw`2. Paste the full contents of `bosonto-workflow-service.jsw`3. Ensure `backend/email-gateway.jsw` exists (email sending dependency)### Step 3: Verify Collections ExistThe service uses these Wix Data collections:| Collection | Purpose ||------------|---------|| `Members` | Main membership records || `BosontoCRM` | Event-specific CRM data || `BosontoPipeline` | Pipeline stage tracking || `BosontoAuditLog` | All admin actions logged || `BosontoBatchQueue` | Batch processing queue || `BosontoSettings` | System settings (test mode, etc.) || `SentEmails` | Email send history || `Admins` | Admin authorization |### Step 4: Run in Test Mode1. Test mode is ON by default ΓÇö all emails route to `ranadhir.ghosh@gmail.com`2. From the admin portal or Wix console, call:   ```javascript   import { runFullPipeline } from 'backend/bosonto-workflow-service.jsw';   const result = await runFullPipeline();   ```3. Check your test email for the clean, member-facing emails4. Verify: no agent/pipeline info, correct amounts, family names shown, verify prompts present### Step 5: Go LiveWhen ready for production:```javascriptimport { toggleTestMode } from 'backend/bosonto-workflow-service.jsw';await toggleTestMode(false); // Emails now go to actual members```---## Email Types Sent| Email | Recipient | When ||-------|-----------|------|| **Membership Confirmation** | Paid members | After payment matched || **Contact Verification** | Paid members | Asks for missing phone/address, confirms spouse || **Payment Reminder** | Evite-Yes unpaid | Shows pricing table, previous year context, Zelle instructions |All emails include:- ΓÜá∩╕Å "Please verify" block- Family/spouse data with confidence-based verification requests- Correct FULL household amounts (not halved)- No agent, pipeline, stage, or technical terminology---## Spouse OverridesTo add more confirmed spouse mappings, edit the `SPOUSE_OVERRIDES` object in the service:```javascriptconst SPOUSE_OVERRIDES = {  'ranadhir.ghosh@gmail.com': { spouseName: 'Moumita Mukherjee (Ghosh)', spouseEmail: 'moumita.mukherje@gmail.com' },  // Add more as needed:  // 'member@email.com': { spouseName: 'Full Name', spouseEmail: 'spouse@email.com' }};```---## Local Testing (Node.js)The local executor is still available for testing without Wix:```bashnode bosonto-pipeline-executor.js# Outputs: bosonto-live-dashboard.html (118 KB)# All emails ΓåÆ ranadhir.ghosh@gmail.com (TEST MODE)```