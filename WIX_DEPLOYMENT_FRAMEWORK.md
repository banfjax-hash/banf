# ≡ƒÄ» WIX DEPLOYMENT FRAMEWORK - Structured Approach**Purpose:** Follow Wix's recommended development, testing, and production workflow  **Created:** February 20, 2026  **Project:** BANF Bengali Association Website  ---## ≡ƒôï PHASE OVERVIEW```ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé SETUP PHASE Γöé DEV PHASE    Γöé TEST PHASE   Γöé PROD PHASE   ΓöéΓö£ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöñΓöé ΓÇó Validate  Γöé ΓÇó Local dev  Γöé ΓÇó Full test  Γöé ΓÇó Publish    ΓöéΓöé ΓÇó Clean     Γöé ΓÇó Build      Γöé ΓÇó Verify     Γöé ΓÇó Monitor    ΓöéΓöé ΓÇó Configure Γöé ΓÇó Test logic Γöé ΓÇó CollectionsΓöé ΓÇó Stabilize  ΓöéΓöé ΓÇó Audit     Γöé ΓÇó Services   Γöé ΓÇó Backend    Γöé ΓÇó Optimize   ΓöéΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö┤ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö┤ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö┤ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ```---## PHASE 1: SETUP & VALIDATION### Step 1.1: Repository Validation**Goal:** Ensure clean, correct code version```bash# Check both reposcd c:\projects\banf\banf1-wixgit log --oneline -1# Expected: Latest commit with collections setup# Compare with mirrorcd c:\projects\banf\banf1-mirrorgit log --oneline -1# Should match exactly```**Checkpoint:** Γ£à Both repos on commit `8f08930` (LATEST)### Step 1.2: Fresh Environment Setup**Goal:** Clean local environment```bashcd c:\projects\banf\banf1-wix# Remove node_modules and lock files to start freshrm -r node_modulesrm package-lock.json# Reinstall dependenciesnpm install# Verify Wix CLI versionnpx wix --version# Expected: v1.1.163 or higher```### Step 1.3: Configuration Audit**Goal:** Verify all configurations are correct```bash# Check wix.config.jsoncat wix.config.json# Should have: siteId: c13ae8c5-7053-4f2d-9a9a-371869be4395# Check .npmrc (should use public registry)cat .npmrc# Should NOT have corporate proxy# Verify package.json has wix CLIcat package.json | grep "wix"```### Step 1.4: Authentication**Goal:** Ensure Wix authentication is valid```bash# Login to Wixnpx wix login# Verify accountnpm run wix:auth-status  # or check wix.config.json for siteId```**Checkpoint:** Γ£à All configurations validated---## PHASE 2: LOCAL DEVELOPMENT & TESTING### Step 2.1: Start Local Development Environment**Goal:** Run Wix local dev server```bash# Kill any existing processesGet-Process node -ErrorAction SilentlyContinue | Stop-Process -Force# Start local dev mode$env:Path = "$env:Path;C:\Program Files\nodejs"npx.cmd wix dev# Expected output:#   Γ£ö Success Development environment ready#   Γ£ö Success Development environment ready  #   Γ£ö Local files have been synced```**This opens:**- Local Editor: https://editor.wix.com (with wsUrl parameter)- Local Server: http://localhost:57319 (or assigned port)### Step 2.2: Verify Backend Code**Goal:** Ensure all 51 backend modules are present and valid```bash# Check backend directoryls -R src/backend/ | wc -l# Should show ~51 .jsw files# List critical modulesls src/backend/ | grep -E "(http-functions|sponsors|events|magazine|setup)"# Must have: http-functions.js, sponsors.jsw, events.jsw, magazine.jsw, banf-setup.jsw```**Check for syntax errors:**```bash# Look for common issues in backend filesgrep -r "import.*from" src/backend/ | head -10grep -r "export.*function" src/backend/ | head -10```### Step 2.3: Test Core HTTP Endpoints**Goal:** Verify API endpoints work correctlyFrom browser console (F12 ΓåÆ Console):```javascript// Test #1: Check if endpoints are availablefetch('/_functions/setup_collections')  .then(r => r.json())  .then(d => console.log('Γ£à Endpoint available:', d))  .catch(e => console.log('Γ¥î Error:', e.message));// Test #2: Check events datafetch('/_functions/get_events')  .then(r => r.json())  .then(d => console.log('Γ£à Events endpoint works, items:', d.items?.length || 0))  .catch(e => console.log('Γ¥î Error:', e.message));// Test #3: Check sponsors datafetch('/_functions/get_sponsors')  .then(r => r.json())  .then(d => console.log('Γ£à Sponsors endpoint works, items:', d.sponsors?.length || 0))  .catch(e => console.log('Γ¥î Error:', e.message));```### Step 2.4: Database Collections Status**Goal:** Check which collections existFrom browser console:```javascript// Check collections via Velo APIimport wixData from 'wix-data';const collections = ['Sponsors', 'Events', 'Members', 'Magazine', 'RadioSchedule', 'Announcements', 'Volunteers'];(async () => {  for (const col of collections) {    try {      const count = await wixData.query(col).count();      console.log(`Γ£à ${col}: ${count} items`);    } catch (e) {      console.log(`Γ¥î ${col}: Does not exist yet`);    }  }})();```**Expected Results:**- Γ¥î Most collections don't exist yet (this is normal)- This shows which need to be created in next phase### Step 2.5: Unit Test - Sample Operations**Goal:** Test basic CRUD if collections exist```javascript// Try to create a test item (will fail if no collection, that's OK)import wixData from 'wix-data';(async () => {  try {    const result = await wixData.insert('TestCollection', {       name: 'Test',       date: new Date()     });    console.log('Γ£à Insert works:', result._id);  } catch (e) {    console.log('Γä╣∩╕Å  No TestCollection yet (expected):', e.message);  }})();```**Checkpoint:** Γ£à Backend code validated, endpoints ready---## PHASE 3: COLLECTIONS SETUP & DATA TESTING### Step 3.1: Create Collections in Wix Admin**Goal:** Set up all 7 core collections**Navigate to:** https://www.wix.com/dashboard/c13ae8c5-7053-4f2d-9a9a-371869be4395/cms**Create These Collections:**#### 1. Sponsors```Fields:- companyName (Text)- tier (Text)- website (URL)- description (RichText)- status (Text)```#### 2. Events  ```Fields:- title (Text)- date (DateTime)- location (Text)- description (RichText)- status (Text)```#### 3. Members```Fields:- name (Text)- email (Text)- phone (Text)- membershipType (Text)- joinDate (DateTime)- status (Text)```#### 4. Magazine```Fields:- title (Text)- issueNumber (Number)- publishDate (DateTime)- content (RichText)```#### 5. RadioSchedule```Fields:- timeSlot (Text)- showName (Text)- description (Text)- isLive (Boolean)```#### 6. Announcements```Fields:- title (Text)- content (RichText)- date (DateTime)```#### 7. Volunteers```Fields:- name (Text)- email (Text)- phone (Text)- skills (Text)- availability (Text)```### Step 3.2: Verify Collections in Local Dev**Goal:** Confirm collections are accessible from local environmentIn browser console (local editor):```javascriptimport wixData from 'wix-data';(async () => {  const collections = ['Sponsors', 'Events', 'Members', 'Magazine', 'RadioSchedule', 'Announcements', 'Volunteers'];    console.log('≡ƒôª Verifying collections...\n');  let created = 0;  let missing = 0;    for (const col of collections) {    try {      const count = await wixData.query(col).limit(1).find();      console.log(`Γ£à ${col}: Accessible (${count.totalCount} items)`);      created++;    } catch (e) {      console.log(`Γ¥î ${col}: Not accessible`);      missing++;    }  }    console.log(`\n${'ΓòÉ'.repeat(40)}`);  console.log(`Γ£à Ready: ${created}/7  Γ¥î Waiting: ${missing}/7`);})();```### Step 3.3: Populate Sample Data**Goal:** Add initial data to collectionsRun setup module in browser console:```javascriptimport { setupAllCollections } from 'backend/banf-setup.jsw';setupAllCollections()  .then(result => {    console.log('Γ£à Setup Complete!', result);    alert('≡ƒÄë All collections populated with sample data!');  })  .catch(error => {    console.error('Γ¥î Setup failed:', error);    alert('Setup error: ' + error.message);  });```**Expected Results:**- All 7 collections created- Sample data: Sponsors, Events, Members, Magazine, Radio Shows, Announcements, Volunteers- No errors in console### Step 3.4: Data Validation Tests**Goal:** Verify data quality```javascriptimport wixData from 'wix-data';(async () => {  console.log('≡ƒôè Data Quality Check\n');    // Test 1: Sponsors  const sponsors = await wixData.query('Sponsors').find();  console.log(`Γ£à Sponsors: ${sponsors.items.length} records`);  console.log('   Sample:', sponsors.items[0].companyName);    // Test 2: Events  const events = await wixData.query('Events').find();  console.log(`Γ£à Events: ${events.items.length} records`);  console.log('   Sample:', events.items[0].title);    // Test 3: Members  const members = await wixData.query('Members').find();  console.log(`Γ£à Members: ${members.items.length} records`);  console.log('   Sample:', members.items[0].name);    // Test 4: Magazine  const magazine = await wixData.query('Magazine').find();  console.log(`Γ£à Magazine: ${magazine.items.length} records`);    // Test 5: RadioSchedule  const radio = await wixData.query('RadioSchedule').find();  console.log(`Γ£à RadioSchedule: ${radio.items.length} records`);    // Test 6: Announcements  const announcements = await wixData.query('Announcements').find();  console.log(`Γ£à Announcements: ${announcements.items.length} records`);    // Test 7: Volunteers  const volunteers = await wixData.query('Volunteers').find();  console.log(`Γ£à Volunteers: ${volunteers.items.length} records`);    console.log('\n' + 'ΓòÉ'.repeat(40));  console.log('Γ£à All collections populated!');})();```**Checkpoint:** Γ£à All 7 collections created with sample data---## PHASE 4: COMPREHENSIVE TESTING### Step 4.1: Page Element Testing**Goal:** Verify all page elements display correctlyFrom local editor browser:```javascript// Check if key elements are presentconst tests = {  'Featured Events': () => document.querySelector('[data-element="featured-events"]') !== null,  'Sponsors Section': () => document.querySelector('[data-element="sponsors"]') !== null,  'Radio Schedule': () => document.querySelector('[data-element="radio-schedule"]') !== null,  'Magazine Preview': () => document.querySelector('[data-element="magazine"]') !== null,  'Announcements': () => document.querySelector('[data-element="announcements"]') !== null};Object.entries(tests).forEach(([name, test]) => {  console.log(`${test() ? 'Γ£à' : 'Γ¥î'} ${name}`);});```### Step 4.2: Responsive Design Testing**Goal:** Test mobile, tablet, desktop views**In local editor:**1. Use DevTools device emulation (F12 ΓåÆ Device toolbar)2. Test breakpoints: 375px (mobile), 768px (tablet), 1024px (desktop)3. Verify:   - Γ£à Navigation responsive   - Γ£à Images load correctly   - Γ£à Content readable   - Γ£à Forms functional### Step 4.3: Performance Testing**Goal:** Check loading speed and resource usage```javascript// Performance checkconsole.time('Page Load');fetch(window.location.href)  .then(r => r.text())  .then(html => {    console.timeEnd('Page Load');        // Count resources    const images = document.querySelectorAll('img').length;    const scripts = document.querySelectorAll('script').length;        console.log(`≡ƒôè Resources: ${images} images, ${scripts} scripts`);  });// Check Core Web Vitals readinessconsole.log(`ΓÜí Performance baseline ready for testing`);```### Step 4.4: Backend Service Testing**Goal:** Verify all HTTP endpoints work```bash# Test from terminal$env:Path = "$env:Path;C:\Program Files\nodejs"# Test endpoint availabilityInvoke-WebRequest -Uri "http://localhost:57319/_functions/get_events" -UseBasicParsing | ConvertFrom-Json | Select-Object -ExpandProperty items -First 1Invoke-WebRequest -Uri "http://localhost:57319/_functions/get_sponsors" -UseBasicParsing | ConvertFrom-Json```**Checkpoint:** Γ£à All tests passing, no errors---## PHASE 5: PUBLISHING TO PRODUCTION### Step 5.1: Pre-Publish Checklist**Goal:** Final validation before publishing```BEFORE Publishing - Verify these:Backend Code:- [ ] 51 .jsw files present in src/backend/- [ ] http-functions.js has correct endpoints- [ ] No syntax errors in .jsw files- [ ] All imports resolve correctlyDatabase:- [ ] All 7 collections created in Wix CMS- [ ] Collections have correct field names (exact match)- [ ] Sample data populated (at least 1 item per collection)- [ ] No empty collectionsConfiguration:- [ ] wix.config.json has correct siteId- [ ] .npmrc uses public registry (no proxy)- [ ] package.json dependencies installed- [ ] .gitignore excludes node_modulesTesting:- [ ] Local dev environment working- [ ] All endpoints tested and working- [ ] Page displays correctly- [ ] Collections accessible from frontend code- [ ] No console errorsAuthentication:- [ ] Logged in to correct Wix account (banfjax@gmail.com)- [ ] Site ID verified- [ ] Publishing credentials valid```### Step 5.2: Clean Publish Build**Goal:** Publish without errors```bash$env:Path = "$env:Path;C:\Program Files\nodejs"cd c:\projects\banf\banf1-wix# Stop local dev if runningGet-Process node -ErrorAction SilentlyContinue | Stop-Process -Force# Verify clean statedir src/backend/ | Measure-Object | Select Count# Publishnpx.cmd wix publish -y# Expected output:# Γ£ö Success Deployed site document# Γ£ö Success Site updated successfully```**If error "Failed to deploy site document":**- This is a known temporary Wix error- The site still publishes (check at https://banfwix.wixsite.com/banf1)- Retry if needed, but don't worry if you see this error### Step 5.3: Verify Production Site**Goal:** Confirm live site is working```bash# Check from terminalInvoke-WebRequest -Uri "https://banfwix.wixsite.com/banf1" -UseBasicParsing | Select-Object StatusCode# Expected: 200 OK```Visit in browser: https://banfwix.wixsite.com/banf1**Verify:**- Γ£à Site loads- Γ£à No 404 errors- Γ£à Events display- Γ£à Sponsors display- Γ£à Content shows correctly### Step 5.4: Production Data Verification**Goal:** Confirm collections are live and accessibleFrom browser console on **production site**:```javascriptfetch('/_functions/get_events')  .then(r => r.json())  .then(d => {    console.log('Γ£à Events from production:', d.items?.length || 0);  });fetch('/_functions/get_sponsors')  .then(r => r.json())  .then(d => {    console.log('Γ£à Sponsors from production:', d.sponsors?.length || 0);  });```**Checkpoint:** Γ£à Site live and functional---## PHASE 6: POST-PRODUCTION MONITORING### Step 6.1: Monitor Errors**Goal:** Watch for any issues```bash# Check production site healthInvoke-WebRequest -Uri "https://banfwix.wixsite.com/banf1" -Verbose 2>&1 | grep -E "(Error|Warning|Success)"```### Step 6.2: Backup & Documentation**Goal:** Save working state```bash# Tag the working commitcd c:\projects\banf\banf1-wixgit tag -a v1.0-production -m "BANF site live with all collections"git push origin --tags# Document the stateecho "Production deployment successful on $(date)" > DEPLOYMENT_STATUS.txt```### Step 6.3: Ongoing Maintenance**Goal:** Plan updates```When making changes:1. Make changes in local dev (npx wix dev)2. Test in local editor3. Test against collections4. Publish when ready (npx wix publish -y)5. Verify on production site6. Tag in git if major version```---## ≡ƒôè COMPLETE WORKFLOW SUMMARY```ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ PHASE 1: SETUP ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Validate repos (both synced Γ£à)  Γöé ΓÇó Fresh npm install            Γöé ΓÇó Verify configs & auth        ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ             Γ¼çΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ PHASE 2: LOCAL DEV ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Start local dev (wix dev)    Γöé ΓÇó Verify 51 backend modules    Γöé ΓÇó Test HTTP endpoints          Γöé ΓÇó Check collection status      ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ             Γ¼çΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ PHASE 3: COLLECTIONS ΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Create 7 collections in Wix  Γöé ΓÇó Verify access from local     Γöé ΓÇó Populate sample data (setup) Γöé ΓÇó Validate data quality        ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ             Γ¼çΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ PHASE 4: TESTING ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Element testing              Γöé ΓÇó Responsive design            Γöé ΓÇó Performance baseline         Γöé ΓÇó Backend endpoints            ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ             Γ¼çΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ PHASE 5: PUBLISH ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Pre-publish checklist        Γöé ΓÇó Clean publish build          Γöé ΓÇó Verify production site       Γöé ΓÇó Check live data              ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ             Γ¼çΓöîΓöÇΓöÇΓöÇΓöÇ PHASE 6: MONITOR ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉΓöé ΓÇó Error monitoring             Γöé ΓÇó Backup & tags                Γöé ΓÇó Maintenance plan             ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ```---## Γ£à SUCCESS CRITERIA| Phase | Success Criteria | Status ||-------|-----------------|--------|| 1 | All configs validated | ΓÅ│ || 2 | Backend code verified, endpoints working | ΓÅ│ || 3 | All 7 collections created + populated | ΓÅ│ || 4 | All tests passing, no errors | ΓÅ│ || 5 | Site live at https://banfwix.wixsite.com/banf1 | Γ£à || 6 | Monitoring & backup in place | ΓÅ│ |---## ≡ƒÜÇ NEXT IMMEDIATE ACTIONS1. **Run Phase 1 validation** (5 min)2. **Start Phase 2 local dev** (2 min)3. **Execute Phase 3 collections** (15 min)4. **Run Phase 4 tests** (10 min)5. **Publish Phase 5** (5 min)**Total Time:** ~45 minutes to complete full workflow---*Framework created: February 20, 2026*  *Project: BANF Bengali Association Website*  *Following: Wix Development Best Practices*