#!/usr/bin/env node/** * FINAL CRM Fix ΓÇö Strict payment matching (no false fuzzy matches) * Only match if FULL NAME matches (both first AND last name). */const fs = require('fs');// PRICINGconst PRICING = {  m2: { family: 375, couple: 330, individual: 205, student: 145, name: 'M2 Premium' },  m1: { family: 280, couple: 255, individual: 140, student: 100, name: 'M1 Regular' },  cultural: { family: 180, couple: 140, individual: 100, student: 75, name: 'Cultural Special' },  guest: { family: 50, couple: 35, individual: 25, student: 15, name: 'Guest Pass' }};function guessTier(amount) {  for (const [tier, prices] of Object.entries(PRICING)) {    for (const [type, price] of Object.entries(prices)) {      if (typeof price === 'number' && price === amount) return { tier, type, price, tierName: prices.name };    }  }  return null;}// Payments (all 10 Zelle after Feb 27)const payments = [  { name: 'RANADHIR GHOSH', amount: 375, source: 'zelle', date: 'Sun, 1 Mar 2026 14:01:10 -0800' },  { name: 'AMIT CHANDAK', amount: 375, source: 'zelle', date: 'Sun, 1 Mar 2026 18:03:55 -0800' },  { name: 'SUVANKAR PAUL', amount: 330, source: 'zelle', date: 'Sun, 1 Mar 2026 18:17:30 -0800' },  { name: 'SUNETRA BASU GHOSH', amount: 330, source: 'zelle', date: 'Sun, 1 Mar 2026 18:22:38 -0800' },  { name: 'Amit Kumar Saha', amount: 330, source: 'zelle', date: 'Sun, 1 Mar 2026 18:28:56 -0800' },  { name: 'TARIT K MONDAL', amount: 205, source: 'zelle', date: 'Mon, 2 Mar 2026 07:14:09 -0800' },  { name: 'FNU AMRITA', amount: 375, source: 'zelle', date: 'Mon, 2 Mar 2026 11:12:20 -0800' },  { name: 'ASOK CHAUDHURI', amount: 130, source: 'zelle', date: 'Mon, 2 Mar 2026 12:48:09 -0800' },  { name: 'Ishita Saha', amount: 145, source: 'zelle', date: 'Wed, 4 Mar 2026 20:44:37 -0800' },  { name: 'SUVENDU MAITRA', amount: 280, source: 'zelle', date: 'Thu, 5 Mar 2026 17:01:49 -0800' },];// Manual payment ΓåÆ RSVP guest mapping (strict, verified by human knowledge + name analysis)const PAYMENT_MAP = {  'RANADHIR GHOSH':     'Ranadhir',         // Ranadhir Ghosh ΓåÆ Ranadhir (Evite username)  'AMIT CHANDAK':       'Amit Chandak',  'SUVANKAR PAUL':      'Suvankar Paul',  'SUNETRA BASU GHOSH': 'Sunetra Basu Ghosh',  'Amit Kumar Saha':    null,                // Not in Evite RSVP-Yes (need to check if spouse paid)  'TARIT K MONDAL':     'Tarit Mondal',  'FNU AMRITA':         null,                // Unknown ΓÇö possibly spouse of Amit Chandak  'ASOK CHAUDHURI':     'Asok Chaudhuri',  'Ishita Saha':        'Ishita Saha',  'SUVENDU MAITRA':     null,                // Not in Evite RSVP-Yes ΓÇö possibly Slmaitra? (RSVP No)};// Evite RSVP-Yes (47 guests from scan)const scanV2 = JSON.parse(fs.readFileSync('bosonto-deep-scan-v2.json', 'utf8'));const eviteYes = scanV2.bosonto.attendeesYes;const eviteNo = scanV2.bosonto.attendeesNo || [];// Load CRMconst crmRaw = JSON.parse(fs.readFileSync('banf-crm-reconciliation.json', 'utf8'));const members = crmRaw.members || crmRaw;console.log('ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ');console.log('  STRICT CRM FIX ΓÇö No False Payment Matches');console.log('ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ');// Step 1: Clear ALL Bosonto event attendancefor (const m of members) {  if (m.eventAttendance) {    m.eventAttendance = m.eventAttendance.filter(e => e.eventName !== 'Bosonto Utsob 2026');  }}// Step 2: Reset ALL payment data (start fresh for 2026-27)for (const m of members) {  if (m.paymentStatus === 'paid' || m.paymentStatus === 'paid_fuzzy') {    m.paymentStatus = 'not_paid';    m.paymentAmount = 0;    m.paymentSource = '';    m.paymentDate = '';    m.paymentMatchType = '';  }}// Step 3: Helper to find CRM memberfunction findMember(name) {  if (!name) return null;  const norm = name.toLowerCase().replace(/[^a-z\s]/g, '').trim();    for (const m of members) {    const mName = (m.displayName || m.fullName || `${m.firstName || ''} ${m.lastName || ''}`.trim()).toLowerCase().replace(/[^a-z\s]/g, '').trim();    if (mName && mName === norm) return m;  }  // Partial: first+last name parts must all match  const parts = norm.split(/\s+/).filter(p => p.length > 2);  if (parts.length >= 2) {    for (const m of members) {      const mName = (m.displayName || m.fullName || `${m.firstName || ''} ${m.lastName || ''}`.trim()).toLowerCase();      if (parts.every(p => mName.includes(p))) return m;    }  }  return null;}// Step 4: Add Bosonto RSVP datafor (const att of eviteYes) {  let member = findMember(att.name);  if (!member) {    member = {      email: '',      displayName: att.name,      firstName: att.name.split(' ')[0] || '',      lastName: att.name.split(' ').slice(1).join(' ') || '',      membershipTier: 'guest',      membershipStatus: 'evite_rsvp',      paymentStatus: 'not_paid',      paymentAmount: 0,      eventAttendance: []    };    members.push(member);  }  if (!member.eventAttendance) member.eventAttendance = [];  member.eventAttendance.push({    eventName: 'Bosonto Utsob 2026',    rsvp: 'yes',    adults: att.adults,    kids: att.kids,    dietary: 'unknown'  });}// Add No RSVPsfor (const att of eviteNo) {  let member = findMember(att.name);  if (member) {    if (!member.eventAttendance) member.eventAttendance = [];    member.eventAttendance.push({      eventName: 'Bosonto Utsob 2026', rsvp: 'no', adults: 0, kids: 0    });  }}// Step 5: Apply STRICT payment matchingconsole.log('\n  ΓöÇΓöÇ PAYMENT MATCHING (strict) ΓöÇΓöÇ');let matchedPayments = 0, unmatchedPayments = [];for (const p of payments) {  const eviteName = PAYMENT_MAP[p.name];    if (eviteName === null) {    // Unresolved payment ΓÇö not matched to any Evite RSVP-Yes guest    unmatchedPayments.push(p);    console.log(`  ΓÜá∩╕Å  UNMATCHED: $${p.amount} from ${p.name} ΓÇö no RSVP-Yes guest found`);    continue;  }    // Find the member  const member = findMember(eviteName);  if (member) {    member.paymentStatus = 'paid';    member.paymentAmount = p.amount;    member.paymentSource = p.source;    member.paymentDate = p.date;    member.paymentMatchType = 'strict_verified';    const tierInfo = guessTier(p.amount);    if (tierInfo) {      member.membershipTier = tierInfo.tier;      member.householdType = tierInfo.type;    }    matchedPayments++;    console.log(`  Γ£à $${p.amount} ${p.name.padEnd(22)} ΓåÆ ${eviteName.padEnd(22)} [${tierInfo ? tierInfo.tier + '/' + tierInfo.type : 'unknown tier'}]`);  } else {    console.log(`  Γ¥î $${p.amount} ${p.name} ΓåÆ mapped to "${eviteName}" but NOT FOUND in CRM!`);    unmatchedPayments.push(p);  }}// Step 6: Final statslet yesCount = 0, noCount = 0, paidCount = 0, unpaidCount = 0;let totalA = 0, totalK = 0;const attendeesFinal = [];for (const m of members) {  const bEvt = (m.eventAttendance || []).find(e => e.eventName === 'Bosonto Utsob 2026');  if (!bEvt) continue;  if (bEvt.rsvp === 'yes') {    yesCount++;    totalA += bEvt.adults || 0;    totalK += bEvt.kids || 0;    const isPaid = m.paymentStatus === 'paid';    if (isPaid) paidCount++; else unpaidCount++;    attendeesFinal.push({      name: m.displayName || `${m.firstName || ''} ${m.lastName || ''}`.trim(),      email: m.email || '',      adults: bEvt.adults || 0,      kids: bEvt.kids || 0,      paymentStatus: m.paymentStatus || 'not_paid',      paymentAmount: m.paymentAmount || 0,      membershipTier: m.membershipTier || 'guest',      householdType: m.householdType || ''    });  } else {    noCount++;  }}console.log('\n' + 'ΓòÉ'.repeat(65));console.log('  FINAL BOSONTO UTSOB 2026 ΓÇö CORRECTED DATA');console.log('ΓòÉ'.repeat(65));console.log(`  RSVP Yes: ${yesCount} | No: ${noCount}`);console.log(`  Headcount: ${totalA} adults + ${totalK} kids = ${totalA + totalK}`);console.log(`  Paid: ${paidCount} ($${attendeesFinal.filter(a => a.paymentStatus === 'paid').reduce((s, a) => s + a.paymentAmount, 0)})`);console.log(`  Not paid: ${unpaidCount}`);console.log(`  Unmatched payments: ${unmatchedPayments.length} ($${unmatchedPayments.reduce((s, p) => s + p.amount, 0)})`);console.log('\n  ΓöÇΓöÇ PAID ATTENDEES (verified) ΓöÇΓöÇ');attendeesFinal.filter(a => a.paymentStatus === 'paid').sort((a, b) => a.name.localeCompare(b.name)).forEach(a => {  console.log(`    Γ£à ${a.name.padEnd(28)} $${String(a.paymentAmount).padStart(5)} ${(a.membershipTier + '/' + a.householdType).padEnd(15)} A:${a.adults} K:${a.kids}`);});console.log('\n  ΓöÇΓöÇ UNPAID ATTENDEES ΓöÇΓöÇ');attendeesFinal.filter(a => a.paymentStatus !== 'paid').sort((a, b) => a.name.localeCompare(b.name)).forEach((a, i) => {  console.log(`    ${String(i+1).padStart(3)}. ${a.name.padEnd(28)} ${(a.email || '').padEnd(30)} A:${a.adults} K:${a.kids} [${a.membershipTier}]`);});console.log('\n  ΓöÇΓöÇ UNMATCHED PAYMENTS (need manual resolution) ΓöÇΓöÇ');unmatchedPayments.forEach(p => {  const tierInfo = guessTier(p.amount);  console.log(`    ΓÜá∩╕Å  $${p.amount} from ${p.name} [${tierInfo ? tierInfo.tier + '/' + tierInfo.type : '?'}] ΓÇö ${p.date}`);});// SavecrmRaw.members = members;crmRaw.lastUpdated = new Date().toISOString();crmRaw.membershipYear = '2026-27';crmRaw.paymentCutoffDate = '2026-02-27';crmRaw.bosonto_final = {  scanDate: new Date().toISOString(),  rsvpYes: yesCount, rsvpNo: noCount,  headcount: { adults: totalA, kids: totalK, total: totalA + totalK },  paid: paidCount, unpaid: unpaidCount,  totalPayments: payments.length,  matchedPayments: matchedPayments,  unmatchedPayments: unmatchedPayments.length,  unmatchedDetails: unmatchedPayments.map(p => ({ name: p.name, amount: p.amount }))};fs.writeFileSync('banf-crm-reconciliation.json', JSON.stringify(crmRaw, null, 2));console.log('\nΓ£à CRM saved with strict payment matching');