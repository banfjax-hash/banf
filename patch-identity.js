#!/usr/bin/env node/** * BANF Multi-Dimensional Identity System Patch *  * Applies all changes to admin-portal.html: * 1. Adds identity dimensions to CRM (childrenNames, spouseName, joinTimestamp, membershipYears) * 2. Converts USERS to multi-role model (roles[] instead of single roleId) * 3. Adds IDENTITY_GRAPH data structure * 4. Adds identity resolution engine functions * 5. Adds Identity panel in sidebar + HTML * 6. Updates user assignment for multi-role * 7. Updates renderUsers for multi-role display * 8. Updates E2E test for multi-role */const fs = require('fs');const path = require('path');const PORTAL_PATH = path.join(__dirname, 'banf1-wix', 'src', 'public', 'admin-portal.html');let html = fs.readFileSync(PORTAL_PATH, 'utf8');const origLen = html.length;// Helper: replace first occurrencefunction rep(old, nw, label) {  if (!html.includes(old)) {    console.error(`[FAIL] ${label}: Could not find marker text (${old.substring(0, 80)}...)`);    return false;  }  html = html.replace(old, nw);  console.log(`[OK] ${label}`);  return true;}// ====================================================================// 1. UPDATE CRM MEMBERS WITH IDENTITY DIMENSIONS// ====================================================================// Replace each CRM member to add identity fieldsconst crmReplacements = [  // MBR-001  [    `profession:'Software Architect',city:'Jacksonville',state:'FL'},`,    `profession:'Software Architect',city:'Jacksonville',state:'FL',childrenNames:['Ria','Arjun'],spouseName:'Soma Ghosh',joinTimestamp:'2024-08-15T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-001 identity dims'  ],  // MBR-002  [    `profession:'Physician',city:'Jacksonville',state:'FL'},`,    `profession:'Physician',city:'Jacksonville',state:'FL',childrenNames:['Aritra'],spouseName:'Rupa Sen',joinTimestamp:'2023-01-10T00:00:00Z',membershipYears:['2023','2024','2025','2026']},`,    'CRM MBR-002 identity dims'  ],  // MBR-003  [    `profession:'CPA',city:'Jacksonville',state:'FL'},`,    `profession:'CPA',city:'Jacksonville',state:'FL',childrenNames:[],spouseName:'Amit Bose',joinTimestamp:'2024-03-20T00:00:00Z',membershipYears:['2024','2025','2026']},`,    'CRM MBR-003 identity dims'  ],  // MBR-004  [    `profession:'Teacher',city:'Jacksonville',state:'FL'},`,    `profession:'Teacher',city:'Jacksonville',state:'FL',childrenNames:['Sohini','Sourav'],spouseName:'Moumita Das',joinTimestamp:'2022-06-01T00:00:00Z',membershipYears:['2022','2023','2024','2025','2026']},`,    'CRM MBR-004 identity dims'  ],  // MBR-005  [    `profession:'Artist',city:'Jacksonville',state:'FL'},`,    `profession:'Artist',city:'Jacksonville',state:'FL',childrenNames:[],spouseName:'',joinTimestamp:'2025-01-05T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-005 identity dims'  ],  // MBR-006  [    `profession:'Engineer',city:'Jacksonville',state:'FL'},`,    `profession:'Engineer',city:'Jacksonville',state:'FL',childrenNames:['Diya'],spouseName:'Anita Mukherjee',joinTimestamp:'2023-09-12T00:00:00Z',membershipYears:['2023','2024','2025','2026']},`,    'CRM MBR-006 identity dims'  ],  // MBR-007  [    `profession:'Marketing',city:'Jacksonville',state:'FL'},`,    `profession:'Marketing',city:'Jacksonville',state:'FL',childrenNames:['Tiya'],spouseName:'Keya Chatterjee',joinTimestamp:'2024-02-28T00:00:00Z',membershipYears:['2024','2025']},`,    'CRM MBR-007 identity dims'  ],  // MBR-008  [    `profession:'IT Specialist',city:'Jacksonville',state:'FL'},`,    `profession:'IT Specialist',city:'Jacksonville',state:'FL',childrenNames:[],spouseName:'',joinTimestamp:'2025-04-10T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-008 identity dims'  ],  // MBR-009  [    `profession:'Student',city:'Jacksonville',state:'FL'},`,    `profession:'Student',city:'Jacksonville',state:'FL',childrenNames:[],spouseName:'',joinTimestamp:'2025-06-15T00:00:00Z',membershipYears:['2026']},`,    'CRM MBR-009 identity dims'  ],  // MBR-010  [    `profession:'Pharmacist',city:'Jacksonville',state:'FL'},`,    `profession:'Pharmacist',city:'Jacksonville',state:'FL',childrenNames:['Kunal','Kavya'],spouseName:'Rekha Gupta',joinTimestamp:'2022-11-20T00:00:00Z',membershipYears:['2023','2024','2025','2026']},`,    'CRM MBR-010 identity dims'  ],  // MBR-011  [    `profession:'Business Owner',city:'Jacksonville',state:'FL'},`,    `profession:'Business Owner',city:'Jacksonville',state:'FL',childrenNames:['Shreya'],spouseName:'Tapan Dey',joinTimestamp:'2024-07-01T00:00:00Z',membershipYears:['2024','2025','2026']},`,    'CRM MBR-011 identity dims'  ],  // MBR-012  [    `profession:'Dentist',city:'Jacksonville',state:'FL'},`,    `profession:'Dentist',city:'Jacksonville',state:'FL',childrenNames:[],spouseName:'Puja Saha',joinTimestamp:'2025-02-14T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-012 identity dims'  ],  // MBR-013  [    `profession:'Nurse',city:'Jacksonville',state:'FL'},`,    `profession:'Nurse',city:'Jacksonville',state:'FL',childrenNames:['Neel','Nisha'],spouseName:'Sourav Chakraborty',joinTimestamp:'2024-10-05T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-013 identity dims'  ],  // MBR-014  [    `profession:'Retired',city:'Jacksonville',state:'FL'},`,    `profession:'Retired',city:'Jacksonville',state:'FL',childrenNames:['Sumon'],spouseName:'Swapna Ghosh',joinTimestamp:'2020-03-15T00:00:00Z',membershipYears:['2020','2021','2022','2023','2024']},`,    'CRM MBR-014 identity dims'  ],  // MBR-015  [    `profession:'Lawyer',city:'Jacksonville',state:'FL'},`,    `profession:'Lawyer',city:'Jacksonville',state:'FL',childrenNames:['Prithvi'],spouseName:'Debashis Sarkar',joinTimestamp:'2024-11-28T00:00:00Z',membershipYears:['2025','2026']},`,    'CRM MBR-015 identity dims'  ],];for (const [old, nw, label] of crmReplacements) {  rep(old, nw, label);}// ====================================================================// 2. CONVERT USERS TO MULTI-ROLE MODEL + ADD IDENTITY_GRAPH// ====================================================================// Find the old USERS line + DRIVE_LIST and replace with new multi-role model + identity graphconst oldUsersBlock = `let USERS = [  {name:'Ranadhir Ghosh',email:'ranadhir.ghosh@gmail.com',roleId:'super-admin',access:'full',invited:`;// Find itconst usersIdx = html.indexOf(`let USERS = [`);if (usersIdx === -1) {  console.error('[FAIL] Could not find USERS array');} else {  // Find from USERS to DRIVE_LIST  const driveListIdx = html.indexOf('let DRIVE_LIST = [];', usersIdx);  if (driveListIdx === -1) {    console.error('[FAIL] Could not find DRIVE_LIST');  } else {    const oldBlock = html.substring(usersIdx, driveListIdx);    const newBlock = `// MULTI-ROLE USER MODEL// Users can have MANY roles. Once credentials are set, they persist across all drives.// Identity is linked via identityId to the IDENTITY_GRAPH for disambiguation.let USERS = [  {name:'Ranadhir Ghosh',email:'ranadhir.ghosh@gmail.com',   roles:[{id:'super-admin',name:'Super Admin',assignedDate:'2024-08-15',context:'system',status:'active'}],   roleHistory:[{roleId:'super-admin',roleName:'Super Admin',from:'2024-08-15',to:null,action:'assigned',by:'System'}],   credentials:{username:'ranadhir.ghosh',hasPassword:true},   identityId:'IDN-001',access:'full',invited:null,status:'active',signedUp:true},];// MULTI-DIMENSIONAL IDENTITY GRAPH// Resolves "who is this person?" using multiple dimensions when names are not unique.// Dimensions: email (primary, immutable), phone, familyId, childrenNames, spouseName (mutable!),//             joinTimestamp, membershipYears, city, profession// NOTE: Date of birth is NOT stored per privacy policy.// Strategy: Each dimension has a weight. Composite score >= 80 = auto-match, 50-79 = suggest, <50 = new identity.let IDENTITY_GRAPH = [  {identityId:'IDN-001',primaryEmail:'ranadhir.ghosh@gmail.com',displayName:'Ranadhir Ghosh',   dimensions:{emails:['ranadhir.ghosh@gmail.com'],phones:['904-555-0101'],familyId:'FAM-2025-A1',     childrenNames:['Ria','Arjun'],spouseName:'Soma Ghosh',joinTimestamp:'2024-08-15T00:00:00Z',     membershipYears:['2025','2026'],city:'Jacksonville',profession:'Software Architect'},   confidenceScore:100,linkedMemberIds:['MBR-001'],linkedUserEmails:['ranadhir.ghosh@gmail.com'],   createdAt:'2024-08-15',lastVerified:new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})},  {identityId:'IDN-002',primaryEmail:'president@banf.org',displayName:'Arun Sen',   dimensions:{emails:['president@banf.org'],phones:['904-555-0102'],familyId:'FAM-2025-A2',     childrenNames:['Aritra'],spouseName:'Rupa Sen',joinTimestamp:'2023-01-10T00:00:00Z',     membershipYears:['2023','2024','2025','2026'],city:'Jacksonville',profession:'Physician'},   confidenceScore:100,linkedMemberIds:['MBR-002'],linkedUserEmails:[],   createdAt:'2023-01-10',lastVerified:new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})},  {identityId:'IDN-003',primaryEmail:'subir.ghosh@gmail.com',displayName:'Subir Ghosh',   dimensions:{emails:['subir.ghosh@gmail.com'],phones:['904-555-0114'],familyId:'FAM-2025-D2',     childrenNames:['Sumon'],spouseName:'Swapna Ghosh',joinTimestamp:'2020-03-15T00:00:00Z',     membershipYears:['2020','2021','2022','2023','2024'],city:'Jacksonville',profession:'Retired'},   confidenceScore:100,linkedMemberIds:['MBR-014'],linkedUserEmails:[],   createdAt:'2020-03-15',lastVerified:new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})},];let IDENTITY_COUNTER = 4;`;    html = html.substring(0, usersIdx) + newBlock + html.substring(driveListIdx);    console.log('[OK] Replaced USERS with multi-role model + IDENTITY_GRAPH');  }}// ====================================================================// 3. ADD IDENTITY SIDEBAR ITEM// ====================================================================rep(  `<div class="sb-item" data-panel="users"><i class="fas fa-users-cog"></i><span>User Management</span></div>`,  `<div class="sb-item" data-panel="users"><i class="fas fa-users-cog"></i><span>User Management</span></div>    <div class="sb-item" data-panel="identity"><i class="fas fa-fingerprint"></i><span>Identity Engine</span></div>`,  'Sidebar: Identity Engine item');// ====================================================================// 4. ADD IDENTITY PANEL HTML (after User Management panel, before Stakeholder Drive)// ====================================================================const stakeholderPanelMarker = `<!-- \u00E2\u0095\u0090\u00E2\u0095\u0090\u00E2\u0095\u0090\u00E2\u0095\u0090\u00E2\u0095\u0090\u00E2\u0095\u0090\u00E2\u0095\u0090 STAKEHOLDER DRIVE`;// Actually let's find the stakeholder drive section more reliablyconst shDriveIdx = html.indexOf('panel-stakeholder-drive');if (shDriveIdx === -1) {  console.error('[FAIL] Could not find stakeholder drive panel');} else {  // Find the <!-- before it  let searchBack = shDriveIdx;  while (searchBack > 0 && html.substring(searchBack - 4, searchBack) !== '<!--') {    searchBack--;  }  // Go back a bit more to get the start of line  while (searchBack > 0 && html[searchBack - 1] !== '\n') {    searchBack--;  }    const identityPanel = `    <!-- ======= IDENTITY ENGINE ======= -->    <div class="portal-section" id="panel-identity">      <div class="stg-label"><i class="fas fa-circle" style="color:var(--cyan)"></i> Multi-Dimensional Identity</div>            <div class="card-a">        <h2><i class="fas fa-fingerprint"></i> Identity Resolution Engine</h2>        <p style="font-size:.78rem;color:var(--muted);margin-bottom:8px">          <strong>Strategy:</strong> Since the same first + last name can exist in the community and date of birth cannot be stored,           identity is resolved using <strong>multi-dimensional matching</strong>: email (primary), phone, family ID, children's names,           join timestamp, membership year history, city, and profession. Spouse name is treated as <em>mutable</em> (lower weight).        </p>        <div class="privacy-banner" style="background:rgba(6,182,212,.06);border-color:rgba(6,182,212,.2);color:var(--cyan)">          <i class="fas fa-shield-alt"></i>          <div><strong>Identity Dimensions (No DOB Policy)</strong> &mdash; Date of birth is never collected or stored.           Identity is resolved via composite scoring across 9 dimensions. Score &ge;80 = auto-match, 50-79 = manual review, &lt;50 = new identity.</div>        </div>      </div>      <div class="card-a">        <h2><i class="fas fa-search"></i> Identity Lookup / Disambiguation</h2>        <p style="font-size:.78rem;color:var(--muted);margin-bottom:8px">Search by name to test multi-dimensional identity resolution. Useful when same first+last name exists.</p>        <div class="form-row">          <div class="form-group" style="flex:2"><label>Search Name</label><input type="text" id="identity-search" placeholder="e.g. Ghosh (will find Ranadhir Ghosh AND Subir Ghosh)"></div>          <div class="form-group"><label>&nbsp;</label><button class="btn-primary" id="btn-identity-search"><i class="fas fa-search me-1"></i>Resolve Identity</button></div>        </div>        <div id="identity-results" style="margin-top:12px"></div>      </div>      <div class="card-a">        <h2><i class="fas fa-project-diagram"></i> Identity Graph (${String.fromCharCode(60)}span id="identity-count"${String.fromCharCode(62)}0${String.fromCharCode(60)}/span${String.fromCharCode(62)} identities)</h2>        <table class="t"><thead><tr><th>ID</th><th>Name</th><th>Primary Email</th><th>Dimensions</th><th>Confidence</th><th>Linked Members</th><th>Roles</th><th>Verified</th></tr></thead>        <tbody id="identity-graph-body"></tbody></table>      </div>      <div class="card-a">        <h2><i class="fas fa-layer-group"></i> Dimension Weights</h2>        <table class="t"><thead><tr><th>Dimension</th><th>Weight</th><th>Type</th><th>Notes</th></tr></thead>        <tbody>          <tr><td><i class="fas fa-envelope me-1" style="color:var(--green)"></i><strong>Email Address</strong></td><td><span class="badge-s badge-green">100%</span></td><td>Primary Key</td><td>Immutable. Exact match = instant identity.</td></tr>          <tr><td><i class="fas fa-phone me-1" style="color:var(--blue)"></i><strong>Phone Number</strong></td><td><span class="badge-s badge-blue">80%</span></td><td>Strong Signal</td><td>Exact match on phone across records.</td></tr>          <tr><td><i class="fas fa-people-roof me-1" style="color:var(--cyan)"></i><strong>Family ID</strong></td><td><span class="badge-s badge-cyan">70%</span></td><td>Strong Signal</td><td>Same familyId links household members.</td></tr>          <tr><td><i class="fas fa-child me-1" style="color:var(--purple)"></i><strong>Children's Names</strong></td><td><span class="badge-s badge-purple">60%</span></td><td>Supporting</td><td>Each matching child adds 60% / count. Stable dimension.</td></tr>          <tr><td><i class="fas fa-user-tag me-1" style="color:var(--yellow)"></i><strong>Name + City + Profession</strong></td><td><span class="badge-s badge-yellow">50%</span></td><td>Supporting</td><td>Composite: name match + same city + same profession.</td></tr>          <tr><td><i class="fas fa-clock me-1" style="color:var(--orange)"></i><strong>Join Timestamp</strong></td><td><span class="badge-s badge-orange">40%</span></td><td>Time Signature</td><td>Registration within 30 days = match. Unique temporal fingerprint.</td></tr>          <tr><td><i class="fas fa-calendar-check me-1" style="color:var(--blue)"></i><strong>Membership Year History</strong></td><td><span class="badge-s badge-blue">35%</span></td><td>Supporting</td><td>Overlap in membership years indicates same person.</td></tr>          <tr><td><i class="fas fa-heart me-1" style="color:var(--pink)"></i><strong>Spouse Name</strong></td><td><span class="badge-s badge-dim">20%</span></td><td>Mutable</td><td>Lower weight because spouse name can change. Fuzzy match.</td></tr>          <tr><td><i class="fas fa-birthday-cake me-1" style="color:var(--red)"></i><strong>Date of Birth</strong></td><td><span class="badge-s badge-red">N/A</span></td><td>EXCLUDED</td><td>NOT stored per BANF privacy policy. Identity resolved without DOB.</td></tr>        </tbody></table>      </div>      <div class="card-a">        <h2><i class="fas fa-clock-rotate-left"></i> Role History &amp; Credential Persistence</h2>        <p style="font-size:.78rem;color:var(--muted);margin-bottom:8px">          A user can hold <strong>multiple roles simultaneously</strong>. Once credentials are set (username/password),           they persist across <strong>all drives</strong> (membership, EC, stakeholder). Role changes are tracked in history.        </p>        <div id="role-history-panel"></div>      </div>    </div>`;  html = html.substring(0, searchBack) + identityPanel + html.substring(searchBack);  console.log('[OK] Added Identity Engine panel HTML');}// ====================================================================// 5. REPLACE USER ASSIGNMENT - MULTI-ROLE SUPPORT// ====================================================================// Replace the assign-role click handlerconst oldAssignHandler = `document.getElementById('btn-assign-role').addEventListener('click',()=>{  const name=document.getElementById('assign-panel').dataset.memberName;  const email=document.getElementById('assign-email').value;  const roleId=document.getElementById('assign-role').value;  const access=document.getElementById('assign-access').value;  if(!roleId){alert('Please select a role.');return;}  if(USERS.find(u=>u.email===email)){alert('User already registered.');return;}  USERS.push({name,email,roleId,access,invited`;const assignIdx = html.indexOf(`document.getElementById('btn-assign-role').addEventListener('click',()=>{`);if (assignIdx === -1) {  console.error('[FAIL] Could not find btn-assign-role handler');} else {  // Find the end of this handler (next document.getElementById('btn-assign-invite'))  const nextHandlerMarker = `document.getElementById('btn-assign-invite')`;  const nextIdx = html.indexOf(nextHandlerMarker, assignIdx);  if (nextIdx === -1) {    console.error('[FAIL] Could not find btn-assign-invite handler');  } else {    // Go back to find the start of the line    let endOfAssignHandler = nextIdx;    while (endOfAssignHandler > 0 && html[endOfAssignHandler - 1] === '\n') {      endOfAssignHandler--;    }    // Actually we need to find where the assign handler ends. Let's look for });  before the invite handler    // The simplest approach: find the last }); before nextIdx    let searchPos = nextIdx - 1;    while (searchPos > assignIdx) {      if (html.substring(searchPos, searchPos + 3) === '});\n' || html.substring(searchPos, searchPos + 3) === '});') {        break;      }      searchPos--;    }    // searchPos now points to the }); of the assign handler    const oldAssign = html.substring(assignIdx, searchPos + 3);        const newAssignHandler = `document.getElementById('btn-assign-role').addEventListener('click',()=>{  const name=document.getElementById('assign-panel').dataset.memberName;  const email=document.getElementById('assign-email').value;  const roleId=document.getElementById('assign-role').value;  const access=document.getElementById('assign-access').value;  if(!roleId){alert('Please select a role.');return;}  // MULTI-ROLE: If user exists, ADD role instead of blocking  const existingUser=USERS.find(u=>u.email===email);  if(existingUser){    if(existingUser.roles.find(r=>r.id===roleId)){alert('User already has this role.');return;}    const role=roleById(roleId);    existingUser.roles.push({id:roleId,name:role?role.name:roleId,assignedDate:new Date().toISOString().split('T')[0],context:'manual',status:'active'});    existingUser.roleHistory.push({roleId,roleName:role?role.name:roleId,from:new Date().toISOString().split('T')[0],to:null,action:'role-added',by:'Super Admin'});    addLog('USER_ADD','Added role '+((role&&role.name)||roleId)+' to existing user '+name+' (multi-role)');    renderUsers();renderAll();    document.getElementById('assign-panel').style.display='none';crmSearchInput.value='';    return;  }  // New user: create with multi-role structure  const role=roleById(roleId);  const identityId=resolveOrCreateIdentity(email,name);  USERS.push({name,email,    roles:[{id:roleId,name:role?role.name:roleId,assignedDate:new Date().toISOString().split('T')[0],context:'manual',status:'active'}],    roleHistory:[{roleId,roleName:role?role.name:roleId,from:new Date().toISOString().split('T')[0],to:null,action:'assigned',by:'Super Admin'}],    credentials:{username:email.split('@')[0],hasPassword:false},    identityId:identityId,access,invited:null,status:'active',signedUp:false});  addLog('USER_ADD','Assigned '+name+' as '+(role?role.name:roleId)+' (identity: '+identityId+')');  renderUsers();renderAll();  document.getElementById('assign-panel').style.display='none';crmSearchInput.value='';});`;        html = html.substring(0, assignIdx) + newAssignHandler + html.substring(searchPos + 3);    console.log('[OK] Replaced btn-assign-role handler with multi-role support');  }}// Now replace btn-assign-invite handler  const inviteIdx = html.indexOf(`document.getElementById('btn-assign-invite').addEventListener('click',()=>{`);if (inviteIdx === -1) {  console.error('[FAIL] Could not find btn-assign-invite handler');} else {  // Find the end - look for the });  followed by function renderUsers  const renderUsersMarker = `function renderUsers()`;  const renderUsersIdx = html.indexOf(renderUsersMarker, inviteIdx);  if (renderUsersIdx === -1) {    console.error('[FAIL] Could not find renderUsers function');  } else {    let endInvite = renderUsersIdx;    // Go back to find });    while (endInvite > inviteIdx && html.substring(endInvite - 4, endInvite) !== '});\n') {      endInvite--;    }        const oldInvite = html.substring(inviteIdx, endInvite);        const newInviteHandler = `document.getElementById('btn-assign-invite').addEventListener('click',()=>{  const name=document.getElementById('assign-panel').dataset.memberName;  const email=document.getElementById('assign-email').value;  const roleId=document.getElementById('assign-role').value;  const access=document.getElementById('assign-access').value;  if(!roleId){alert('Select a role first.');return;}  const member=CRM.find(m=>m.email===email);  if(member && !member.emailOptIn){    alert('Privacy Block: '+name+' has emailOptIn=false.\\nCannot send invitation email.\\n\\nThe member must opt-in via the data correction form first.');    return;  }  // MULTI-ROLE: If user exists, add role  const existingUser=USERS.find(u=>u.email===email);  const role=roleById(roleId);  if(existingUser){    if(existingUser.roles.find(r=>r.id===roleId)){alert('User already has this role.');return;}    existingUser.roles.push({id:roleId,name:role?role.name:roleId,assignedDate:new Date().toISOString().split('T')[0],context:'drive-invite',status:'active'});    existingUser.roleHistory.push({roleId,roleName:role?role.name:roleId,from:new Date().toISOString().split('T')[0],to:null,action:'role-added-invite',by:'Super Admin'});    addLog('USER_ADD','Added role '+(role?role.name:roleId)+' to '+name+' via invite (multi-role, credentials persist)');    addLog('EMAIL','Communication Agent: sendGmail() -> '+email+' with data privacy notice, unsubscribe link');    renderUsers();renderAll();    document.getElementById('assign-panel').style.display='none';    alert('Role added to existing user '+name+'\\nCredentials remain the same (persistent identity).\\nInvitation email sent.');    return;  }  // New user with invite  const identityId=resolveOrCreateIdentity(email,name);  USERS.push({name,email,    roles:[{id:roleId,name:role?role.name:roleId,assignedDate:new Date().toISOString().split('T')[0],context:'drive-invite',status:'active'}],    roleHistory:[{roleId,roleName:role?role.name:roleId,from:new Date().toISOString().split('T')[0],to:null,action:'assigned-invite',by:'Super Admin'}],    credentials:{username:email.split('@')[0],hasPassword:false},    identityId:identityId,    access,invited:new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}),status:'invited',signedUp:false});  addLog('USER_ADD','Assigned & invited '+name+' as '+(role?role.name:roleId)+' (identity: '+identityId+')');  addLog('EMAIL','Communication Agent: sendGmail() -> '+email+' with data privacy notice, unsubscribe link');  renderUsers();renderAll();  document.getElementById('assign-panel').style.display='none';  alert('Invitation sent to '+name+' ('+email+')\\nRole: '+(role?role.name:roleId)+'\\nIdentity: '+identityId+'\\nAccess: '+access+'\\n\\nEmail includes data privacy notice + unsubscribe link.\\nOnce they sign up, credentials persist across ALL drives.');});`;    html = html.substring(0, inviteIdx) + newInviteHandler + html.substring(endInvite);    console.log('[OK] Replaced btn-assign-invite handler with multi-role support');  }}// ====================================================================// 6. REPLACE renderUsers FOR MULTI-ROLE DISPLAY// ====================================================================const renderUsersStart = html.indexOf('function renderUsers(){');if (renderUsersStart === -1) {  console.error('[FAIL] Could not find renderUsers function');} else {  const removeUserStart = html.indexOf('window.removeUser=', renderUsersStart);  if (removeUserStart === -1) {    console.error('[FAIL] Could not find removeUser function');  } else {    // Find end of removeUser    let endRemoveUser = html.indexOf('};', removeUserStart);    if (endRemoveUser !== -1) endRemoveUser += 2;        const newRenderUsers = `function renderUsers(){  document.getElementById('users-body').innerHTML=USERS.map((u,i)=>{    // Multi-role: show ALL roles as badges    var roleBadges=u.roles.map(function(r){      var color=r.id==='super-admin'?'red':r.id.includes('stakeholder')?'orange':r.id.includes('ec')?'blue':'purple';      return '<span class="badge-s badge-'+color+'" style="margin-right:3px" title="Since: '+r.assignedDate+'">'+r.name+'</span>';    }).join('');    var credBadge=u.credentials&&u.credentials.hasPassword?'<span class="badge-s badge-green" title="Credentials set - persist across all drives"><i class="fas fa-key" style="font-size:.55rem"></i></span>':'<span class="badge-s badge-dim" title="No credentials yet"><i class="fas fa-key" style="font-size:.55rem"></i></span>';    var identBadge=u.identityId?'<span class="badge-s badge-cyan" style="font-size:.62rem" title="Identity: '+u.identityId+'">'+u.identityId+'</span>':'';    return '<tr>'+    '<td><strong>'+u.name+'</strong><br>'+identBadge+'</td><td>'+u.email+'</td>'+    '<td>'+roleBadges+'</td>'+    '<td><span class="badge-s badge-dim">'+u.access+'</span> '+credBadge+'</td>'+    '<td>'+(u.invited||'--')+'</td>'+    '<td><span class="badge-s badge-'+(u.status==='active'?'green':u.status==='invited'?'yellow':'dim')+'">'+u.status+(u.signedUp?' &#10003;':'')+'</span></td>'+    '<td>'+(u.roles.every(function(r){return r.id==='super-admin'})?'':'<button class="btn-danger btn-sm" onclick="removeUser('+i+')"><i class="fas fa-trash"></i></button>')+'</td>'+  '</tr>'}).join('');}window.removeUser=function(i){if(confirm('Remove "'+USERS[i].name+'"?')){USERS.splice(i,1);renderUsers();renderAll();}};`;        html = html.substring(0, renderUsersStart) + newRenderUsers + html.substring(endRemoveUser);    console.log('[OK] Replaced renderUsers + removeUser with multi-role version');  }}// ====================================================================// 7. ADD IDENTITY RESOLUTION ENGINE FUNCTIONS (before STAKEHOLDER DRIVE section in JS)// ====================================================================// Find the stakeholder drive section in JSconst stakeholderDriveJS = `const driveSearchInput=document.getElementById('drive-search');`;const stDriveJSIdx = html.indexOf(stakeholderDriveJS);if (stDriveJSIdx === -1) {  console.error('[FAIL] Could not find stakeholder drive JS section');} else {  const identityEngine = `// ==================================================================//  MULTI-DIMENSIONAL IDENTITY RESOLUTION ENGINE// ==================================================================// Dimension weights for identity scoringconst IDENTITY_WEIGHTS = {  email: 100,      // Primary key - exact match = instant identity  phone: 80,       // Strong signal  familyId: 70,    // Strong signal - same household  childrenNames: 60, // Per matching child (divided by count)  nameComposite: 50, // Name + City + Profession together  joinTimestamp: 40,  // Within 30 days = match (time signature)  membershipYears: 35, // Overlapping years  spouseName: 20     // Mutable - lower weight (can change)  // DOB: NOT USED - excluded per privacy policy};function computeIdentityScore(candidateCRM, searchDimensions) {  var score = 0;  var matches = [];  // 1. Email (primary)  if (searchDimensions.email && candidateCRM.email &&       searchDimensions.email.toLowerCase() === candidateCRM.email.toLowerCase()) {    score += IDENTITY_WEIGHTS.email;    matches.push('email(100)');  }  // 2. Phone  if (searchDimensions.phone && candidateCRM.phone &&       searchDimensions.phone.replace(/\\D/g,'') === candidateCRM.phone.replace(/\\D/g,'')) {    score += IDENTITY_WEIGHTS.phone;    matches.push('phone(80)');  }  // 3. Family ID  if (searchDimensions.familyId && candidateCRM.familyId &&       searchDimensions.familyId === candidateCRM.familyId) {    score += IDENTITY_WEIGHTS.familyId;    matches.push('familyId(70)');  }  // 4. Children names  if (searchDimensions.childrenNames && searchDimensions.childrenNames.length > 0 &&       candidateCRM.childrenNames && candidateCRM.childrenNames.length > 0) {    var matchCount = 0;    searchDimensions.childrenNames.forEach(function(cn) {      if (candidateCRM.childrenNames.some(function(cc) { return cc.toLowerCase() === cn.toLowerCase(); })) matchCount++;    });    if (matchCount > 0) {      var childScore = Math.round(IDENTITY_WEIGHTS.childrenNames * matchCount / Math.max(searchDimensions.childrenNames.length, candidateCRM.childrenNames.length));      score += childScore;      matches.push('children(' + childScore + ', ' + matchCount + ' match)');    }  }  // 5. Name + City + Profession composite  var nameMatch = searchDimensions.name && (candidateCRM.displayName || '').toLowerCase().includes(searchDimensions.name.toLowerCase());  var cityMatch = searchDimensions.city && candidateCRM.city && searchDimensions.city.toLowerCase() === candidateCRM.city.toLowerCase();  var profMatch = searchDimensions.profession && candidateCRM.profession && candidateCRM.profession.toLowerCase().includes(searchDimensions.profession.toLowerCase());  if (nameMatch && cityMatch && profMatch) { score += IDENTITY_WEIGHTS.nameComposite; matches.push('name+city+prof(50)'); }  else if (nameMatch && (cityMatch || profMatch)) { score += Math.round(IDENTITY_WEIGHTS.nameComposite * 0.6); matches.push('name+partial(30)'); }  else if (nameMatch) { score += Math.round(IDENTITY_WEIGHTS.nameComposite * 0.3); matches.push('name(15)'); }  // 6. Join timestamp (within 30 days)  if (searchDimensions.joinTimestamp && candidateCRM.joinTimestamp) {    var diff = Math.abs(new Date(searchDimensions.joinTimestamp) - new Date(candidateCRM.joinTimestamp));    var daysDiff = diff / (1000 * 60 * 60 * 24);    if (daysDiff < 1) { score += IDENTITY_WEIGHTS.joinTimestamp; matches.push('joinTime-exact(40)'); }    else if (daysDiff <= 30) { score += Math.round(IDENTITY_WEIGHTS.joinTimestamp * 0.5); matches.push('joinTime-near(20)'); }  }  // 7. Membership years overlap  if (searchDimensions.membershipYears && searchDimensions.membershipYears.length > 0 &&      candidateCRM.membershipYears && candidateCRM.membershipYears.length > 0) {    var overlap = searchDimensions.membershipYears.filter(function(y) { return candidateCRM.membershipYears.includes(y); }).length;    if (overlap > 0) {      var yearScore = Math.round(IDENTITY_WEIGHTS.membershipYears * overlap / Math.max(searchDimensions.membershipYears.length, candidateCRM.membershipYears.length));      score += yearScore;      matches.push('years(' + yearScore + ', ' + overlap + ' overlap)');    }  }  // 8. Spouse name (fuzzy, mutable)  if (searchDimensions.spouseName && candidateCRM.spouseName) {    if (searchDimensions.spouseName.toLowerCase() === candidateCRM.spouseName.toLowerCase()) {      score += IDENTITY_WEIGHTS.spouseName;      matches.push('spouse(20)');    } else if (candidateCRM.spouseName.toLowerCase().includes(searchDimensions.spouseName.toLowerCase().split(' ')[0])) {      score += Math.round(IDENTITY_WEIGHTS.spouseName * 0.5);      matches.push('spouse-partial(10)');    }  }  return { score: Math.min(score, 100), matches: matches, candidate: candidateCRM };}function resolveIdentityFromCRM(searchDimensions) {  var results = CRM.map(function(m) {    return computeIdentityScore(m, searchDimensions);  }).filter(function(r) { return r.score > 0; })    .sort(function(a, b) { return b.score - a.score; });  return results;}function resolveOrCreateIdentity(email, name) {  // Check if identity already exists  var existing = IDENTITY_GRAPH.find(function(ig) { return ig.primaryEmail === email; });  if (existing) return existing.identityId;  // Check CRM  var member = CRM.find(function(m) { return m.email === email; });  var newId = 'IDN-' + String(IDENTITY_COUNTER).padStart(3, '0');  IDENTITY_COUNTER++;  var newIdentity = {    identityId: newId, primaryEmail: email, displayName: name,    dimensions: {      emails: [email],      phones: member ? [member.phone] : [],      familyId: member ? member.familyId : '',      childrenNames: member ? (member.childrenNames || []) : [],      spouseName: member ? (member.spouseName || '') : '',      joinTimestamp: member ? (member.joinTimestamp || new Date().toISOString()) : new Date().toISOString(),      membershipYears: member ? (member.membershipYears || []) : [],      city: member ? member.city : '',      profession: member ? member.profession : ''    },    confidenceScore: member ? 100 : 50,    linkedMemberIds: member ? [member.memberId] : [],    linkedUserEmails: [email],    createdAt: new Date().toISOString().split('T')[0],    lastVerified: new Date().toLocaleDateString('en-US', {month:'short', day:'numeric', year:'numeric'})  };  IDENTITY_GRAPH.push(newIdentity);  addLog('IDENTITY', 'Created identity ' + newId + ' for ' + name + ' (' + email + ') - confidence: ' + newIdentity.confidenceScore + '%');  return newId;}// Identity search UI handlerdocument.getElementById('btn-identity-search').addEventListener('click', function() {  var query = document.getElementById('identity-search').value.trim();  if (!query) { alert('Enter a name to search.'); return; }  var searchDims = { name: query };  var results = resolveIdentityFromCRM(searchDims);  var el = document.getElementById('identity-results');  if (results.length === 0) {    el.innerHTML = '<div style="text-align:center;padding:16px;color:var(--dim)">No matches found for "' + query + '"</div>';    return;  }  // Check if multiple matches with same last name (disambiguation needed)  var highMatches = results.filter(function(r) { return r.score >= 15; });  var needsDisambig = highMatches.length > 1;  el.innerHTML = (needsDisambig ? '<div style="padding:8px 12px;background:rgba(234,179,8,.08);border:1px solid rgba(234,179,8,.2);border-radius:8px;margin-bottom:12px;font-size:.78rem;color:var(--yellow)"><i class="fas fa-exclamation-triangle me-1"></i><strong>Disambiguation Required:</strong> Multiple members match "' + query + '". Review dimensional scores below to identify the correct person. Same names exist in the community.</div>' : '') +    '<table class="t"><thead><tr><th>Member</th><th>Email</th><th>Score</th><th>Confidence</th><th>Matching Dimensions</th><th>Children</th><th>Spouse</th><th>Joined</th></tr></thead><tbody>' +    highMatches.map(function(r) {      var m = r.candidate;      var conf = r.score >= 80 ? 'green' : r.score >= 50 ? 'yellow' : 'red';      var confLabel = r.score >= 80 ? 'Auto-Match' : r.score >= 50 ? 'Review' : 'Low';      return '<tr>' +        '<td><strong>' + m.displayName + '</strong><br><span style="font-size:.68rem;color:var(--dim)">' + m.memberId + ' | ' + m.familyId + '</span></td>' +        '<td>' + m.email + '</td>' +        '<td><strong style="color:var(--' + conf + ')">' + r.score + '%</strong></td>' +        '<td><span class="badge-s badge-' + conf + '">' + confLabel + '</span></td>' +        '<td style="font-size:.7rem">' + r.matches.join(', ') + '</td>' +        '<td style="font-size:.72rem">' + ((m.childrenNames && m.childrenNames.length) ? m.childrenNames.join(', ') : '<span style="color:var(--dim)">none</span>') + '</td>' +        '<td style="font-size:.72rem">' + (m.spouseName || '<span style="color:var(--dim)">--</span>') + '</td>' +        '<td style="font-size:.72rem">' + (m.joinTimestamp ? new Date(m.joinTimestamp).toLocaleDateString('en-US',{month:'short',year:'numeric'}) : '--') + '</td>' +        '</tr>';    }).join('') + '</tbody></table>';});document.getElementById('identity-search').addEventListener('keypress', function(e) { if (e.key === 'Enter') document.getElementById('btn-identity-search').click(); });function renderIdentityGraph() {  document.getElementById('identity-count').textContent = IDENTITY_GRAPH.length;  document.getElementById('identity-graph-body').innerHTML = IDENTITY_GRAPH.map(function(ig) {    var dims = ig.dimensions;    var dimTags = [];    if (dims.emails && dims.emails.length) dimTags.push('<span class="badge-s badge-green">email</span>');    if (dims.phones && dims.phones.length) dimTags.push('<span class="badge-s badge-blue">phone</span>');    if (dims.familyId) dimTags.push('<span class="badge-s badge-cyan">family</span>');    if (dims.childrenNames && dims.childrenNames.length) dimTags.push('<span class="badge-s badge-purple">' + dims.childrenNames.length + ' children</span>');    if (dims.spouseName) dimTags.push('<span class="badge-s badge-dim">spouse</span>');    if (dims.joinTimestamp) dimTags.push('<span class="badge-s badge-orange">time</span>');    if (dims.membershipYears && dims.membershipYears.length) dimTags.push('<span class="badge-s badge-blue">' + dims.membershipYears.length + 'yr</span>');    // Find user roles for this identity    var userRoles = [];    USERS.forEach(function(u) {      if (u.identityId === ig.identityId) {        u.roles.forEach(function(r) { userRoles.push(r); });      }    });    var roleBadges = userRoles.length > 0 ? userRoles.map(function(r) {      var c = r.id === 'super-admin' ? 'red' : r.id.includes('stakeholder') ? 'orange' : 'blue';      return '<span class="badge-s badge-' + c + '">' + r.name + '</span>';    }).join(' ') : '<span style="color:var(--dim);font-size:.7rem">No roles</span>';    return '<tr>' +      '<td><code style="color:var(--cyan)">' + ig.identityId + '</code></td>' +      '<td><strong>' + ig.displayName + '</strong></td>' +      '<td style="font-size:.74rem">' + ig.primaryEmail + '</td>' +      '<td>' + dimTags.join(' ') + '</td>' +      '<td><span class="badge-s badge-' + (ig.confidenceScore >= 80 ? 'green' : ig.confidenceScore >= 50 ? 'yellow' : 'red') + '">' + ig.confidenceScore + '%</span></td>' +      '<td style="font-size:.72rem">' + ig.linkedMemberIds.join(', ') + '</td>' +      '<td>' + roleBadges + '</td>' +      '<td style="font-size:.7rem;color:var(--dim)">' + ig.lastVerified + '</td>' +      '</tr>';  }).join('');}function renderRoleHistory() {  var el = document.getElementById('role-history-panel');  if (!el) return;  el.innerHTML = USERS.map(function(u) {    var historyRows = u.roleHistory ? u.roleHistory.map(function(h) {      return '<tr><td><span class="badge-s badge-' + (h.action.includes('assigned') ? 'green' : h.action.includes('removed') ? 'red' : 'blue') + '">' + h.action + '</span></td>' +        '<td>' + h.roleName + '</td><td>' + h.from + '</td><td>' + (h.to || '<span style="color:var(--green)">current</span>') + '</td><td>' + h.by + '</td></tr>';    }).join('') : '';    var credStatus = u.credentials && u.credentials.hasPassword ?       '<span class="badge-s badge-green"><i class="fas fa-key" style="font-size:.55rem"></i> Credentials Active</span> <span style="font-size:.68rem;color:var(--muted)">Username: ' + u.credentials.username + ' | Persists across all drives</span>' :      '<span class="badge-s badge-dim"><i class="fas fa-key" style="font-size:.55rem"></i> No Password Set</span>';    return '<div style="background:var(--bg2);border:1px solid var(--line);border-radius:8px;padding:14px;margin-bottom:10px">' +      '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px">' +      '<div><strong style="color:#fff">' + u.name + '</strong> <span style="font-size:.72rem;color:var(--muted)">' + u.email + '</span></div>' +      '<div>' + credStatus + '</div></div>' +      '<div style="margin-bottom:6px">' + u.roles.map(function(r) {        var c = r.id === 'super-admin' ? 'red' : r.id.includes('stakeholder') ? 'orange' : 'blue';        return '<span class="badge-s badge-' + c + '" style="margin-right:4px">' + r.name + ' (since ' + r.assignedDate + ')</span>';      }).join('') + '</div>' +      (historyRows ? '<table class="t" style="font-size:.72rem"><thead><tr><th>Action</th><th>Role</th><th>From</th><th>To</th><th>By</th></tr></thead><tbody>' + historyRows + '</tbody></table>' : '') +      '</div>';  }).join('');}`;  html = html.substring(0, stDriveJSIdx) + identityEngine + html.substring(stDriveJSIdx);  console.log('[OK] Added Identity Resolution Engine functions');}// ====================================================================// 8. UPDATE addToDrive - use roles array// ====================================================================// The addToDrive function itself doesn't reference roleId from the user model, // it gets role from ROLES and adds to DRIVE_LIST. This should still work.// But we need to check the drive send logic uses the right role.// ====================================================================// 9. UPDATE renderAll TO INCLUDE IDENTITY// ====================================================================rep(  'renderRoles();renderUsers();renderDrive();renderEC();renderFeedback();renderDevBoard();renderE2E(null);renderLog(\'dash-log\',8);renderLog(\'full-log\');renderDriveStatus();',  'renderRoles();renderUsers();renderDrive();renderEC();renderFeedback();renderDevBoard();renderE2E(null);renderLog(\'dash-log\',8);renderLog(\'full-log\');renderDriveStatus();renderIdentityGraph();renderRoleHistory();',  'renderAll: add identity renders');// ====================================================================// 10. ADD IDENTITY LOG COLOR// ====================================================================rep(  `EC_COMPLETE:'green'`,  `EC_COMPLETE:'green',IDENTITY:'cyan'`,  'Log colors: add IDENTITY');// ====================================================================// 11. UPDATE KPI DASHBOARD// ====================================================================rep(  `{v:USERS.length,k:'Users',cls:'green'},`,  `{v:USERS.length,k:'Users',cls:'green'},    {v:IDENTITY_GRAPH.length,k:'Identities',cls:'cyan'},`,  'Dashboard KPIs: add Identities count');// ====================================================================// 12. UPDATE E2E TEST - FIX roleId REFERENCES// ====================================================================// The E2E test step 2 pushes a role, that's fine.// Step 3 does: u.roleId='technical-lead' - need to change to multi-roleconst oldE2EStep3 = `const u=USERS.find(u=>u.email==='ranadhir.ghosh@gmail.com');    if(u) u.roleId='technical-lead';`;const newE2EStep3 = `const u=USERS.find(u=>u.email==='ranadhir.ghosh@gmail.com');    if(u) {      if(!u.roles.find(function(r){return r.id==='technical-lead'})){        u.roles.push({id:'technical-lead',name:'Technical Lead',assignedDate:new Date().toISOString().split('T')[0],context:'e2e-test',status:'active'});        u.roleHistory.push({roleId:'technical-lead',roleName:'Technical Lead',from:new Date().toISOString().split('T')[0],to:null,action:'assigned-e2e',by:'E2E Test'});      }    }`;rep(oldE2EStep3, newE2EStep3, 'E2E step 3: multi-role assignment');// E2E reset - fix roleId referenceconst oldE2EReset = `const u=USERS.find(u=>u.email==='ranadhir.ghosh@gmail.com');if(u)u.roleId='super-admin';`;const newE2EReset = `const u=USERS.find(u=>u.email==='ranadhir.ghosh@gmail.com');if(u){u.roles=u.roles.filter(function(r){return r.id!=='technical-lead'});u.roleHistory=u.roleHistory.filter(function(h){return h.action!=='assigned-e2e'});}`;rep(oldE2EReset, newE2EReset, 'E2E reset: multi-role cleanup');// ====================================================================// 13. UPDATE USERS TABLE HEADER// ====================================================================rep(  `<h2><i class="fas fa-users-cog"></i> Registered Users & Access</h2>        <table class="t"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Access</th><th>Invited</th><th>Status</th><th>Actions</th></tr></thead>`,  `<h2><i class="fas fa-users-cog"></i> Registered Users & Access <span style="font-size:.68rem;color:var(--cyan);font-weight:400">(Multi-Role Identity System)</span></h2>        <table class="t"><thead><tr><th>Name / Identity</th><th>Email</th><th>Roles (Multi)</th><th>Access / Creds</th><th>Invited</th><th>Status</th><th>Actions</th></tr></thead>`,  'Users table: update headers for multi-role');// ====================================================================// 14. UPDATE VERSION LABEL// ====================================================================rep(  `<small>Super Admin Portal v2.0</small>`,  `<small>Super Admin Portal v3.0 (Multi-Role Identity)</small>`,  'Version: bump to v3.0');// ====================================================================// WRITE// ====================================================================fs.writeFileSync(PORTAL_PATH, html, 'utf8');console.log(`\n=== PATCH COMPLETE ===`);console.log(`Original: ${origLen} chars`);console.log(`Patched:  ${html.length} chars`);console.log(`Delta:    +${html.length - origLen} chars`);