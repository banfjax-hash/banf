import { ok } from 'wix-http-functions';export async function get_setup(request) {    try {        return ok({            headers: { 'Content-Type': 'text/html' },            body: `<html><head><title>BANF Setup</title></head><body style="font-family: Arial; padding: 20px;"><h1>≡ƒÜÇ BANF Collections Setup</h1><p>Click the button below to create all 11 collections:</p><button onclick="runSetup()" style="padding: 10px 20px; font-size: 16px; background: #4CAF50; color: white; border: none; cursor: pointer;">  Create Collections</button><hr><div id="result"></div><script>async function runSetup() {  try {    const btn = event.target;    btn.disabled = true;    btn.textContent = 'ΓÅ│ Creating collections...';        const response = await fetch('/_functions/get_runSetup');    const data = await response.json();        document.getElementById('result').innerHTML = '<pre style="background: #f0f0f0; padding: 10px; border-radius: 5px;">' + JSON.stringify(data, null, 2) + '</pre>';        if (data.success) {      btn.textContent = 'Γ£à Setup Complete!';      alert('≡ƒÄë All collections created successfully!');    }  } catch (error) {    document.getElementById('result').innerHTML = '<pre style="color: red;">' + error.message + '</pre>';    btn.disabled = false;    btn.textContent = 'Create Collections';  }}</script></body></html>            `        });    } catch (error) {        return ok({            headers: { 'Content-Type': 'text/plain' },            body: 'Error: ' + error.message        });    }}