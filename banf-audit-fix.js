/** * banf-audit-fix.js  ΓÇö v1.0.0 * ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ * Comprehensive audit + auto-fix for ALL 169 BANF CRM members. * * What it does: *   1. Loads all XLSX data (2025-26 membership, Family Universe) *   2. Fetches all CRM members + payments + family groups via API *   3. Cross-references each member for 9 categories of issues *   4. Applies fixes via PATCH /crm_member_update + POST /bulk_import_members *   5. Generates member-audit-all.html report * * Usage: *   node banf-audit-fix.js [--dry-run] [--fix] [--html-only] * * Flags: *   --dry-run   Audit only, print findings, do NOT apply any fixes *   --fix       Audit AND apply all safe auto-fixes *   --html-only Re-generate HTML report from last saved audit.json * ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */'use strict';const XLSX    = require('xlsx');const fetch   = require('node-fetch');const fs      = require('fs');const path    = require('path');const API       = 'https://banfwix.wixsite.com/banf1/_functions';const ADMIN     = 'banfjax@gmail.com';const ADMIN_KEY = 'IST.eyJraWQiOiJQb3pIX2FDMiIsImFsZyI6IlJTMjU2In0..GVx8jeX6lw2qF3cTWQJX4hWVs_unIkBJAgywR_sbASHyJhs95w6euuWIRW5CfQ_PSZmCKHw6ma5IpQawGhR79hYUi46_49yAg9fCklP60iJJlPLKdLj6NtOVIoYoc-WsG8nOW_9qo1om08YA-Qh_5O-oZv6oRW2gk7C2eOF5E1pjt0CgmVIRK8z5HvVqlXYftO9NtaSfHh9vhSVPkxVU6jp1OJBsR_UdcdL6Rpiv-bJx0hKJJOfNJMc89oEBiCaAJ4No65-FsGouo2yIYUCsDAQTtBk9rWh3cH8_n-ts0WK57kdtXVKRqQ5g7ch5usUdFAUBTSaviGXpExj5VoTVKQ';const DATA_DIR = path.join(__dirname, 'banf1-wix', 'banf-data_ingest', 'data');const OUT_DIR  = __dirname;const XLSX_2526   = path.join(DATA_DIR, 'BANF Membership 2025 - 26.xlsx');const XLSX_FAM    = path.join(DATA_DIR, 'BANF_Family_Universe_v3.xlsx');const DRY_RUN   = process.argv.includes('--dry-run') || !process.argv.includes('--fix');const HTML_ONLY = process.argv.includes('--html-only');// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// Helpers// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇfunction norm(s) { return (s || '').toLowerCase().replace(/\s+/g, ' ').trim(); }function normName(s) {  return (s || '').replace(/\b(da|di|didi|da's)\b/gi, '').replace(/\s+/g, ' ').trim();}/** Fuzzy name similarity 0..1 */function nameSim(a, b) {  a = norm(normName(a)); b = norm(normName(b));  if (!a || !b) return 0;  if (a === b) return 1;  if (a.startsWith(b) || b.startsWith(a)) return 0.9;  const [shorter, longer] = a.length <= b.length ? [a, b] : [b, a];  if (longer.includes(shorter)) return 0.8;  return 0;}/** Extract first name from combined "Ranadhir Da/ Moumita Di" */function extractFirstName(raw) {  return normName((raw || '').split(/[\/,]/)[0]).split(/\s+/)[0];}async function apiGet(endpoint, params = {}) {  const qs = new URLSearchParams({ ...params, admin_key: ADMIN_KEY }).toString();  const url = `${API}/${endpoint}?${qs}`;  const res = await fetch(url, { headers: { 'x-user-email': ADMIN } });  if (!res.ok) throw new Error(`GET ${endpoint} ΓåÆ ${res.status}: ${await res.text().catch(()=>'')}`);  return res.json();}async function apiPost(endpoint, body) {  const res = await fetch(`${API}/${endpoint}?admin_key=${encodeURIComponent(ADMIN_KEY)}`, {    method: 'POST',    headers: { 'Content-Type': 'application/json', 'x-user-email': ADMIN },    body: JSON.stringify(body)  });  if (!res.ok) {    const txt = await res.text();    throw new Error(`POST ${endpoint} ΓåÆ ${res.status}: ${txt.substring(0, 120)}`);  }  return res.json();}function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// XLSX loaders// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ/** Returns array of plain objects from first sheet (or named sheet) */function loadSheet(filePath, sheetName) {  if (!fs.existsSync(filePath)) { console.warn(`ΓÜá  File not found: ${filePath}`); return []; }  const wb = XLSX.readFile(filePath);  const wsName = sheetName || wb.SheetNames[0];  const ws = wb.Sheets[wsName] || wb.Sheets[wb.SheetNames[0]];  return XLSX.utils.sheet_to_json(ws, { defval: '' });}/** Load 2025-26 membership ΓåÆ map: normalisedFirstName ΓåÆ row */function load2526() {  const rows = loadSheet(XLSX_2526);  const map = new Map(); // key: normalized first name ΓåÆ {row}  for (const row of rows) {    const raw = row['Name'] || '';    if (!raw.trim()) continue;    const fn = norm(extractFirstName(raw));    if (fn) map.set(fn, { ...row, _rawName: raw });    // Also index by full-line norm for exact match attempts    map.set(norm(normName(raw)), { ...row, _rawName: raw });  }  return { rows, map };}/** Parse Category column ΓåÆ familyType, earlyBird, membershipType */function parseCategory(cat) {  const c = norm(cat || '');  const earlyBird = c.includes('eb') || c.includes('early');  let familyType = 'individual';  let membershipType = 'annual';  if      (c.includes('family'))     { familyType = 'family';     membershipType = 'family'; }  else if (c.includes('couple'))     { familyType = 'couple';     membershipType = 'couple'; }  else if (c.includes('individual')) { familyType = 'individual'; membershipType = 'individual'; }  else if (c.includes('student'))    { familyType = 'individual'; membershipType = 'student'; }  else if (c.includes('senior'))     { familyType = 'individual'; membershipType = 'senior'; }  else if (c.includes('life'))       { familyType = 'individual'; membershipType = 'life'; }  return { familyType, membershipType, earlyBird };}/** Load Family Universe ΓåÆ Map: normFirstName ΓåÆ { years: Set, raw, sheets } */function loadFamilyUniverse() {  if (!fs.existsSync(XLSX_FAM)) { console.warn('ΓÜá  XLSX_FAM not found'); return new Map(); }  const wb = XLSX.readFile(XLSX_FAM);  const universe = new Map(); // key: norm first name ΓåÆ { years, raw }  for (const wsName of wb.SheetNames) {    // Read raw (don't defval) so we can detect actual cell values    const rows = XLSX.utils.sheet_to_json(wb.Sheets[wsName], { defval: null });    if (rows.length === 0) continue;    const sampleKeys = Object.keys(rows[0] || {});    // Year columns: keys that look like 2022-23, 2023-24, 2024-25, 2025-26, or 2022, 2023...    const yearKeys = sampleKeys.filter(k => /\b20\d{2}(-\d{2})?\b/.test(String(k)));    // Name column candidates    const nameKeys = ['Name','name','Family','Member','Member Name','Full Name','FamilyName'];    const nameKey = nameKeys.find(k => sampleKeys.includes(k)) || sampleKeys[0];    for (const row of rows) {      const nameVal = String(row[nameKey] || '').trim();      if (!nameVal || nameVal === 'null') continue;      // Active in a year = cell is truthy (number 1, boolean true, string 'yes'/'x'/'Γ£ô', etc.)      const active = yearKeys.filter(y => {        const v = row[y];        if (v === null || v === undefined || v === '') return false;        if (typeof v === 'boolean') return v;        if (typeof v === 'number')  return v !== 0;        const vs = String(v).toLowerCase().trim();        return ['yes','y','x','Γ£ô','true','1','ok','paid','active'].includes(vs);      });      if (active.length === 0) continue;      // Index by first token of first person's name      const key = norm(normName(nameVal.split(/[\/,&]/)[0]));      if (!key) continue;      if (!universe.has(key)) universe.set(key, { years: new Set(), raw: nameVal, sheets: [] });      active.forEach(y => universe.get(key).years.add(String(y)));      universe.get(key).sheets.push(wsName);    }  }  return universe;}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// CRM data fetcher// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇasync function fetchAllMembers() {  console.log('≡ƒôí Fetching all CRM members...');  const all = [];  let page = 1;  while (true) {    const data = await apiGet('crm_members', { limit: 50, page });    const items = data.members || data.items || data || [];    if (!Array.isArray(items) || items.length === 0) break;    all.push(...items);    if (items.length < 50) break;    page++;    await sleep(300);  }  console.log(`   Γ£ö ${all.length} members fetched`);  return all;}async function fetchAllPayments() {  console.log('≡ƒôí Fetching payments (per XLSX-matched member, batched)...');  // No bulk payments endpoint ΓÇö we'll fetch per-member for those appearing in the 2025-26 XLSX  // Returns a Map: memberId ΓåÆ array of payment records (or empty array = API says none)  // NOTE: only populated for XLSX-matched members; for others we use XLSX absence as indicator  return new Map(); // populated lazily per-member in detectIssues if needed}async function fetchMemberPayments(memberId) {  try {    const data = await apiGet('crm_member_payments', { memberId });    return (data.payments || data.items || []);  } catch (e) {    return [];  }}async function fetchAllFamilies() {  // crm_family requires a familyId ΓÇö no bulk endpoint available  // Family type is inferred from XLSX Category column instead  console.log('≡ƒôí Family groups: skipping bulk fetch (using XLSX Category inference instead)');  return [];}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// Issue detector// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇconst YEAR_NAMES = { '2022-23': '2022', '2023-24': '2023', '2024-25': '2024', '2025-26': '2025' };function detectIssues(member, payments2526, xls2526, famUniverse) {  const issues  = [];  const fixes   = {};  const memberId = member.memberId;  const fn = norm(member.firstName || '');  const ln = norm(member.lastName  || '');  const fullNorm = `${fn} ${ln}`.trim();  // 1. Find matching 2025-26 XLSX row  const xlsRow = xls2526.map.get(fn) || xls2526.map.get(fullNorm);  if (xlsRow) {    const catParsed = parseCategory(xlsRow['Category']);    const xlsAmt = parseFloat((xlsRow['Amount'] || '0').toString().replace(/[$,\s]/g, '')) || 0;    // Issue A: Missing 2025-26 payment record    const hasPay  = payments2526.has(memberId);    if (!hasPay && xlsAmt > 0) {      issues.push({ code: 'PAY_MISSING', severity: 'critical',        msg: `No 2025-26 payment record (should be $${xlsAmt} ${xlsRow['Category']})`,        xlsRow });      // Payment creation handled by re-import ΓÇö flag for bulk re-import      fixes._needsPaymentImport = true;      fixes._xlsRow = xlsRow;    }    // Issue B: familyType mismatch    if (catParsed.familyType && member.familyType !== catParsed.familyType) {      issues.push({ code: 'FAMILY_TYPE', severity: 'warning',        msg: `familyType is "${member.familyType}" but XLSX Category says "${catParsed.familyType}"` });      fixes.familyType = catParsed.familyType;    }    // Issue C: membershipType mismatch    if (catParsed.membershipType && member.membershipType !== catParsed.membershipType) {      issues.push({ code: 'MEMBERSHIP_TYPE', severity: 'info',        msg: `membershipType is "${member.membershipType}" but derived from XLSX: "${catParsed.membershipType}"` });      fixes.membershipType = catParsed.membershipType;    }  }  // 2. Check memberSince vs Family Universe  const famEntry = famUniverse.get(fn) || famUniverse.get(fullNorm);  if (famEntry && famEntry.years.size > 0) {    const earliestYear = [...famEntry.years].sort()[0];    const expectedSince = YEAR_NAMES[earliestYear] || earliestYear.split('-')[0];    if (member.memberSince && member.memberSince > expectedSince) {      issues.push({ code: 'MEMBER_SINCE', severity: 'warning',        msg: `memberSince is "${member.memberSince}" but Family Universe shows active since ${earliestYear} (ΓåÆ ${expectedSince})` });      fixes.memberSince = expectedSince;    } else if (!member.memberSince && expectedSince) {      issues.push({ code: 'MEMBER_SINCE_EMPTY', severity: 'warning',        msg: `memberSince is empty; Family Universe shows ${earliestYear} (ΓåÆ ${expectedSince})` });      fixes.memberSince = expectedSince;    }  }  // 3. memberSince set to current year but member has multi-year history  if (member.memberSince === String(new Date().getFullYear()) && famEntry && famEntry.years.size > 1) {    const earliestYear = [...famEntry.years].sort()[0];    const expectedSince = YEAR_NAMES[earliestYear] || earliestYear.split('-')[0];    issues.push({ code: 'MEMBER_SINCE_WRONG', severity: 'warning',      msg: `memberSince appears to be auto-set to current year but member has ${famEntry.years.size} years of history` });    if (expectedSince < member.memberSince) fixes.memberSince = expectedSince;  }  // 4. isECMember flag vs. actual EC roles (can only check if caller passes roles ΓÇö skipped here, done server-side)  // 5. Missing alternateEmail ΓÇö only flaggable if we had another data source; skip for auto-fix  // 6. membershipStatus not 'active' but has 2025-26 payment  if (xlsRow && member.membershipStatus !== 'active') {    issues.push({ code: 'STATUS_INACTIVE', severity: 'warning',      msg: `membershipStatus="${member.membershipStatus}" but member is in 2025-26 XLSX` });    fixes.membershipStatus = 'active';    fixes.isActive = true;  }  // 7. displayName mismatch  const expectedDisplay = [member.firstName, member.lastName].filter(Boolean).join(' ');  if (member.displayName && member.displayName !== expectedDisplay) {    // soft info only    issues.push({ code: 'DISPLAY_NAME', severity: 'info',      msg: `displayName "${member.displayName}" Γëá computed "${expectedDisplay}"` });  }  return { issues, fixes, xlsRow: xlsRow || null };}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// Fixer// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇasync function applyFixes(member, fixes) {  const results = [];  const { _needsPaymentImport, _xlsRow, ...directFixes } = fixes;  // Direct field updates via crm_member_update  if (Object.keys(directFixes).length > 0) {    try {      const res = await apiPost('crm_member_update', {        memberId: member.memberId,        ...directFixes      });      results.push({ type: 'member_update', status: 'ok', fields: Object.keys(directFixes), res });    } catch (e) {      results.push({ type: 'member_update', status: 'error', error: e.message });    }    await sleep(200);  }  // Payment import via bulk_import_members  if (_needsPaymentImport && _xlsRow) {    try {      const res = await apiPost('bulk_import_members', {        rows: [_xlsRow],        profile: 'membership_fees',        year: '2025-26'      });      results.push({ type: 'payment_import', status: 'ok', res });    } catch (e) {      results.push({ type: 'payment_import', status: 'error', error: e.message });    }    await sleep(200);  }  return results;}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// HTML report generator// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇfunction generateReport(auditResults, members, payments, fixResults) {  const now = new Date().toLocaleString();  const total = members.length;  const withIssues = auditResults.filter(r => r.issues.length > 0).length;  const critical = auditResults.filter(r => r.issues.some(i => i.severity === 'critical')).length;  const fixed = fixResults.filter(r => r.fixApplied).length;  // Severity summary  const summaryBySev = { critical: 0, warning: 0, info: 0 };  auditResults.forEach(r => r.issues.forEach(i => summaryBySev[i.severity] = (summaryBySev[i.severity]||0)+1));  // Issue code frequency  const codeFreq = {};  auditResults.forEach(r => r.issues.forEach(i => codeFreq[i.code] = (codeFreq[i.code]||0)+1));  const rows = auditResults.map(r => {    const m = r.member;    const issueCells = r.issues.map(i =>      `<span class="badge ${i.severity}">${i.code}</span>`).join(' ');    const fixCell = r.fixApplied      ? `<span class="badge ok">FIXED</span> ${r.fixResults.map(f=>f.type+':'+f.status).join(', ')}`      : r.issues.length > 0 ? '<span class="badge warning">PENDING</span>' : '<span class="badge ok">OK</span>';    return `<tr>      <td><b>${m.displayName || `${m.firstName} ${m.lastName}`}</b><br>          <small>${m.memberId}</small></td>      <td>${m.email || 'ΓÇö'}</td>      <td>${m.memberSince || 'ΓÇö'}</td>      <td>${m.familyType || 'ΓÇö'}</td>      <td>${m.membershipStatus || 'ΓÇö'}</td>      <td>${r.hasPayment2526 ? 'Γ£à $' + (r.xlsRow ? r.xlsRow['Amount'] : '') : 'Γ¥î'}</td>      <td>${issueCells || '<span class="badge ok">Γ£ô</span>'}</td>      <td>${fixCell}</td>    </tr>`;  }).join('');  const freqRows = Object.entries(codeFreq).sort((a,b)=>b[1]-a[1]).map(([code,cnt]) =>    `<tr><td>${code}</td><td>${cnt}</td></tr>`).join('');  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BANF Member Audit ΓÇö All Members ΓÇö ${now}</title><style>  :root{--c-ok:#16a34a;--c-warn:#d97706;--c-err:#dc2626;--c-info:#3b82f6;--bg:#f8fafc;--card:#fff;--border:#e2e8f0}  body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:var(--bg);margin:0;padding:20px;color:#1e293b}  h1{font-size:1.8rem;color:#1e293b;margin-bottom:4px}  .subtitle{color:#64748b;margin-bottom:24px}  .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:12px;margin-bottom:28px}  .card{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:18px 20px;text-align:center}  .card .num{font-size:2.4rem;font-weight:700;line-height:1}  .card .lbl{font-size:.78rem;color:#64748b;margin-top:4px}  .card.red .num{color:var(--c-err)} .card.amber .num{color:var(--c-warn)} .card.green .num{color:var(--c-ok)}  .card.blue .num{color:var(--c-info)}  table{width:100%;border-collapse:collapse;background:var(--card);border-radius:10px;overflow:hidden;box-shadow:0 1px 4px #0001;margin-bottom:30px}  th{background:#1e293b;color:#fff;padding:10px 12px;font-size:.78rem;text-align:left}  td{padding:9px 12px;font-size:.82rem;border-bottom:1px solid var(--border);vertical-align:top}  tr:hover td{background:#f1f5f9}  .badge{display:inline-block;padding:2px 8px;border-radius:999px;font-size:.7rem;font-weight:600;margin:1px}  .badge.critical{background:#fee2e2;color:var(--c-err)}  .badge.warning{background:#fef3c7;color:#92400e}  .badge.info{background:#dbeafe;color:#1d4ed8}  .badge.ok{background:#d1fae5;color:#065f46}  h2{font-size:1.25rem;margin:28px 0 10px;color:#1e293b;border-bottom:2px solid var(--border);padding-bottom:6px}  .filter-row{display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap}  input[type=text]{padding:7px 12px;border:1px solid var(--border);border-radius:6px;font-size:.85rem;width:200px}  select{padding:7px 12px;border:1px solid var(--border);border-radius:6px;font-size:.85rem}  .ts{font-size:.75rem;color:#94a3b8;margin-top:4px}</style></head><body><h1>≡ƒöì BANF Member Audit Report</h1><div class="subtitle">Generated: ${now} &nbsp;|&nbsp; Mode: ${DRY_RUN ? '≡ƒöì Audit-Only (Dry Run)' : '≡ƒöº Fix Mode'}</div><div class="grid">  <div class="card blue"><div class="num">${total}</div><div class="lbl">Total Members</div></div>  <div class="card ${critical > 0 ? 'red' : 'green'}"><div class="num">${critical}</div><div class="lbl">Critical Issues</div></div>  <div class="card amber"><div class="num">${withIssues}</div><div class="lbl">Members with Issues</div></div>  <div class="card green"><div class="num">${total - withIssues}</div><div class="lbl">Clean Members</div></div>  <div class="card ${summaryBySev.critical > 0 ? 'red' : 'green'}"><div class="num">${summaryBySev.critical || 0}</div><div class="lbl">Critical Flags</div></div>  <div class="card amber"><div class="num">${summaryBySev.warning || 0}</div><div class="lbl">Warnings</div></div>  <div class="card blue"><div class="num">${summaryBySev.info || 0}</div><div class="lbl">Info Items</div></div>  <div class="card green"><div class="num">${fixed}</div><div class="lbl">Fixes Applied</div></div></div><h2>Issue Frequency by Code</h2><table style="max-width:400px"><thead><tr><th>Issue Code</th><th>Count</th></tr></thead><tbody>${freqRows}</tbody></table><h2>All Members ΓÇö Integration Status</h2><div class="filter-row">  <input type="text" id="search" placeholder="Search name / emailΓÇª" oninput="filterTable()">  <select id="sevFilter" onchange="filterTable()">    <option value="">All Severities</option>    <option value="critical">Critical</option>    <option value="warning">Warning</option>    <option value="ok">Clean Only</option>  </select></div><table id="mainTable"><thead><tr>  <th>Member</th><th>Email</th><th>Since</th><th>Family Type</th><th>Status</th>  <th>Payment 2025-26</th><th>Issues</th><th>Fix Status</th></tr></thead><tbody>${rows}</tbody></table><div class="ts">Report generated by banf-audit-fix.js | BANF CRM Platform v5.7.3</div><script>function filterTable() {  const q  = document.getElementById('search').value.toLowerCase();  const sv = document.getElementById('sevFilter').value;  document.querySelectorAll('#mainTable tbody tr').forEach(tr => {    const txt = tr.textContent.toLowerCase();    const matchQ   = !q  || txt.includes(q);    const matchSev = !sv || (sv === 'ok' ? tr.querySelector('.badge.ok') && !tr.querySelector('.badge.critical,.badge.warning')                                         : tr.querySelector('.badge.' + sv));    tr.style.display = (matchQ && matchSev) ? '' : 'none';  });}</script></body></html>`;}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ// MAIN// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇasync function main() {  console.log('\nΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ');  console.log('  BANF Member Audit + Fix  ΓÇö ' + new Date().toLocaleString());  console.log('  Mode:', DRY_RUN ? 'DRY RUN (audit only)' : 'FIX MODE (will apply changes)');  console.log('ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ\n');  if (HTML_ONLY) {    const saved = path.join(OUT_DIR, 'member-audit-all.json');    if (!fs.existsSync(saved)) { console.error('No saved audit.json found. Run without --html-only first.'); process.exit(1); }    const data = JSON.parse(fs.readFileSync(saved, 'utf8'));    const html = generateReport(data.auditResults, data.members, new Map(), data.fixResults);    fs.writeFileSync(path.join(OUT_DIR, 'member-audit-all.html'), html);    console.log('Γ£à HTML report regenerated from saved audit.json');    return;  }  // 1. Load local XLSX data  console.log('≡ƒôé Loading XLSX data...');  const xls2526     = load2526();  const famUniverse = loadFamilyUniverse();  console.log(`   Γ£ö 2025-26 XLSX: ${xls2526.rows.length} rows, ${xls2526.map.size} lookup keys`);  console.log(`   Γ£ö Family Universe: ${famUniverse.size} entries\n`);  // 2. Fetch CRM data  const members  = await fetchAllMembers();  const families = await fetchAllFamilies();  // Build family index  const famIndex = new Map(families.map(f => [f.familyId, f]));  // 3. Find XLSX-matched members and check their payments in batches  console.log('\n≡ƒôí Checking payments for XLSX-matched members (batched)...');  const pay2526 = new Set(); // memberIds with 2025-26 payment  const xlsMatched = members.filter(m => {    const fn = norm(m.firstName || '');    const full = `${fn} ${norm(m.lastName||'')}`.trim();    return xls2526.map.has(fn) || xls2526.map.has(full);  });  console.log(`   Found ${xlsMatched.length} XLSX-matched members ΓÇö checking payments...`);  // Batch 5 at a time  for (let i = 0; i < xlsMatched.length; i += 5) {    const batch = xlsMatched.slice(i, i + 5);    const results = await Promise.allSettled(batch.map(m => fetchMemberPayments(m.memberId)));    results.forEach((r, idx) => {      if (r.status === 'fulfilled' && r.value.length > 0) {        const hasPay2526 = r.value.some(p =>          (p.membershipYear || '').includes('2025') || (p.importSource || '').includes('2526'));        if (hasPay2526) pay2526.add(batch[idx].memberId);      }    });    await sleep(200);  }  console.log(`   Γ£ö ${pay2526.size} members confirmed with 2025-26 payment record`);  // 3. Audit all members  console.log('\n≡ƒöÄ Auditing members...');  const auditResults = [];  let issueCount = 0;  for (const member of members) {    const { issues, fixes, xlsRow } = detectIssues(member, pay2526, xls2526, famUniverse);    const hasPayment2526 = pay2526.has(member.memberId);    auditResults.push({ member, issues, fixes, xlsRow, hasPayment2526, fixApplied: false, fixResults: [] });    if (issues.length > 0) issueCount++;  }  // Stats  console.log(`\n≡ƒôè Audit Summary:`);  console.log(`   Total members : ${members.length}`);  console.log(`   With issues   : ${issueCount}`);  const code2count = {};  auditResults.forEach(r => r.issues.forEach(i => code2count[i.code] = (code2count[i.code]||0)+1));  Object.entries(code2count).sort((a,b)=>b[1]-a[1]).forEach(([k,v]) =>    console.log(`     ${k.padEnd(22)} ${v}`));  // 4. Apply fixes (only if not dry-run)  if (!DRY_RUN) {    console.log('\n≡ƒöº Applying fixes...');    let applied = 0;    for (const r of auditResults) {      if (Object.keys(r.fixes).length === 0) continue;      process.stdout.write(`   Fixing ${r.member.memberId} (${r.member.displayName})ΓÇª `);      r.fixResults = await applyFixes(r.member, r.fixes);      r.fixApplied = r.fixResults.some(f => f.status === 'ok');      console.log(r.fixApplied ? 'Γ£à' : 'Γ¥î ' + r.fixResults.map(f=>f.error).join(', '));      applied++;    }    console.log(`\n   Applied fixes to ${applied} members`);  }  // 5. Save audit JSON + generate HTML  const saveData = { members, auditResults, fixResults: auditResults.filter(r=>r.fixApplied), generatedAt: new Date().toISOString() };  fs.writeFileSync(path.join(OUT_DIR, 'member-audit-all.json'), JSON.stringify(saveData, null, 2));  console.log('\n≡ƒÆ╛ Saved: member-audit-all.json');  const html = generateReport(auditResults, members, pay2526, auditResults);  const htmlPath = path.join(OUT_DIR, 'member-audit-all.html');  fs.writeFileSync(htmlPath, html);  console.log('≡ƒôä Saved: member-audit-all.html');  // 6. Print member-level detail for critical issues  const criticals = auditResults.filter(r => r.issues.some(i => i.severity === 'critical'));  if (criticals.length) {    console.log(`\n≡ƒÜ¿ Critical Issues (${criticals.length} members):`);    criticals.forEach(r => {      const m = r.member;      console.log(`\n   [${m.memberId}] ${m.displayName || m.firstName + ' ' + m.lastName}`);      r.issues.filter(i => i.severity === 'critical').forEach(i =>        console.log(`     Γ¥î ${i.code}: ${i.msg}`));    });  }  console.log('\nΓ£à Audit complete.\n');  return { auditResults, members };}main().catch(e => { console.error('Fatal:', e); process.exit(1); });