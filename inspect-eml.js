const fs = require('fs');['email_header_raw.eml', 'email_logo_raw.eml'].forEach(f => {    const raw = fs.readFileSync(f, 'utf8');    const bodyStart = raw.indexOf('\r\n\r\n');    const body = bodyStart >= 0 ? raw.substring(bodyStart) : raw.substring(raw.indexOf('\n\n'));    console.log('=== ' + f + ' ===');    console.log('Body length:', body.length);    console.log('Full body:');    console.log(body);    const urls = raw.match(/https?:\/\/\S+/g) || [];    console.log('URLs found:', urls.length);    urls.forEach(u => console.log('  ' + u));    console.log();});