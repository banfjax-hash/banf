# BANF E2E Workflow Plan ΓÇö Complete Test Strategy**Version:** 1.0  **Date:** 2026-03-05  **Status:** PLAN (not executing ΓÇö must be approved before any test run)  **Author:** Copilot Agent  ---## CRITICAL SAFETY FINDING ΓÇö READ FIRST> **`TEST_MODE = false` in `ec-onboarding-gate.js` (line 40)**  > When Step 2 (`ec_send_all_invitations`) runs during any test, **real invitation emails are sent to all EC members in the database**, not just the test user.  > This has been happening on every test run ΓÇö this IS the "confusion and frustration" source.### Immediate Blocker Before Any Test```javascript// ec-onboarding-gate.js line 40:const TEST_MODE = false; // MUST be true during all test runs```When `TEST_MODE = true`, all emails are re-routed to `PRESIDENT_EMAIL` (`ranadhir.ghosh@gmail.com`) instead of real EC members. **This flag must be `true` before any test execution.**---## GitHub Actions StatusCurrent status **(all green as of 2026-03-05)**:| Commit  | Workflow                  | Result  | Timestamp           ||---------|---------------------------|---------|---------------------|| 10ca11b | BANF E2E Pipeline Tests   | SUCCESS | 2026-03-05 18:14    || 10ca11b | pages build and deployment| SUCCESS | 2026-03-05 18:13    || 4631dd8 | BANF E2E Pipeline Tests   | SUCCESS | 2026-03-05 18:10    || 4631dd8 | pages build and deployment| SUCCESS | 2026-03-05 18:09    || 29970d8 | BANF E2E Pipeline Tests   | SUCCESS | 2026-03-05 18:07    || fa8a66b | BANF E2E Pipeline Tests   | SUCCESS | 2026-03-05 18:02    |> **Context:** GitHub Actions tests pass because they only call Wix endpoints and check HTTP responses. They do NOT verify whether the actual email was physically delivered correctly (no SMTP verification in CI). So CI "passing" does NOT mean emails are working end-to-end.---## System Architecture (Test Context)```Test Agent (Node.js)  Γöé  Γö£ΓöÇΓöÇΓöÇ HTTPS ΓåÆ www.jaxbengali.org/_functions/{endpoint}  Γöé              Wix Backend (ec-onboarding-gate.js, admin-api.js, email-automation.js)  Γöé                Γöé  Γöé                Γö£ΓöÇΓöÇΓöÇ Wix Collections (AdminRoles, ECYearStatus, GoogleTokens)  Γöé                Γöé  Γöé                ΓööΓöÇΓöÇΓöÇ Gmail API (OAuth2 refresh token)  Γöé                       ΓåÆ sends email via banfjax@gmail.com  Γöé                       ΓåÆ to: EC members (if TEST_MODE=false) or President only (if TEST_MODE=true)  Γöé  ΓööΓöÇΓöÇΓöÇ GitHub API (api.github.com) ΓåÆ checks workflow run status```---## Test Accounts| Account         | Email                        | Role        | Purpose                    ||-----------------|------------------------------|-------------|----------------------------|| `EC_TEST`       | `ranadhir.ghosh@gmail.com`   | `ec_member` | Test EC member onboarding  || `STAKEHOLDER_TEST` | `banfjax@gmail.com`       | `super_admin` | Verify admin exists       |Both are safe to use ΓÇö `ranadhir.ghosh@gmail.com` is the President's personal email, monitored by BANF leadership. No real member data is affected.---## Complete E2E Workflow: EC Member Onboarding### Pre-Flight Safety Checks (Must Pass Before Any Step)**Pre-Flight 1: Verify TEST_MODE is enabled**- **Action:** Read `ec-onboarding-gate.js` line 40 or call a test endpoint- **Pass criteria:** `TEST_MODE = true` confirmed- **Fail action:** STOP. Do not proceed. Edit file, redeploy, wait for propagation.**Pre-Flight 2: Verify site is healthy**- **Endpoint:** `GET /health`- **Expected response:** `{ healthy: true, version: "5.x.x-..." }`- **Expected HTTP status:** 200- **Fail action:** Wait for Wix CDN propagation (up to 2 min) and retry.**Pre-Flight 3: Verify deploy version matches expected commit**- **Endpoint:** `GET /deploy_check`- **Expected response:** `{ version: "5.x.x-...", commit: "..." }`- **Fail action:** Wait for propagation before running any functional tests.**Pre-Flight 4: Verify all 13 endpoints are registered (not 404)**- **Endpoints to check:**  - `POST admin_role_add`  - `POST admin_role_revoke`  - `GET  admin_roles`  - `POST admin_signup_direct`  - `POST admin_set_password`  - `POST admin_save_profile`  - `POST admin_onboard_complete`  - `POST admin_verify_login`  - `POST admin_onboard_verify`  - `POST ec_send_all_invitations`  - `POST ec_signup_congratulations`  - `GET  admin_dashboard`  - `GET  seed_system`- **Expected:** Every endpoint returns HTTP Γëá 404- **Common failure mode:** Endpoint added to `ec-onboarding-gate.js` but NOT re-exported in `http-functions.js`- **Fix:** Add missing import/re-export in `banf-wix-linked/src/backend/http-functions.js`---### Stage 0: Cleanup (Idempotent Reset)**Purpose:** Start from a clean state so tests are repeatable.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_role_revoke`           || Headers      | `x-user-email: banfjax@gmail.com`              || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "role": "ec_member" }` || Expected HTTP| 200                                            || Expected JSON| `{ "message": "..." }` OR error if role not found || Fail behavior| Either outcome (200 or error) is acceptable. Log and continue. |**Agent output:** `Cleanup: 200 ΓÇö Role revoked` or `Cleanup: 400 ΓÇö No record found`---### Stage 1: Add EC Role**Purpose:** Register the test user as an EC member in the `AdminRoles` collection. This must NOT send a generic welcome email (EC members get a dedicated invitation via `ec_send_all_invitations`).| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_role_add`              || Headers      | `x-user-email: banfjax@gmail.com`              || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "role": "ec_member", "firstName": "Ranadhir", "lastName": "Ghosh", "ecTitle": "President" }` || Expected HTTP| 200                                            || Expected JSON fields | `success: true`, `setupToken: <string>`, `onboardUrl: <string containing "banfjax-hash.github.io/banf/">` || EC-specific assertions | `emailSent: false`, `emailSkipped: <string containing "ec_send_all_invitations">` |**Critical assertion ΓÇö email isolation:**  `emailSent` MUST be `false` for EC members. If it is `true`, the old stakeholder welcome email is still being sent to EC members. This causes EC members to receive both a generic welcome AND the dedicated EC invitation ΓÇö confusing.**Common failure modes:**1. `emailSent: true` ΓåÆ EC branching logic missing or not deployed. Fix: check `admin-api.js` EC conditional branch, redeploy.2. `setupToken: null` ΓåÆ Wix collection write failed (permissions or quota). Check `AdminRoles` collection access.3. `onboardUrl` contains `www.jaxbengali.org` ΓåÆ URL constant not updated. Fix: search for old domain in `admin-api.js`.4. HTTP 403 ΓåÆ `x-user-email` header not recognized or super_admin role missing from `AdminRoles` collection.---### Stage 2: Send EC Invitations**Purpose:** Trigger the dedicated EC invitation email. With TEST_MODE=true, email goes ONLY to President (`ranadhir.ghosh@gmail.com`). With TEST_MODE=false (CURRENT STATE), email goes to ALL EC members in the database.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/ec_send_all_invitations`     || Headers      | `x-user-email: banfjax@gmail.com`              || Body         | `{}`                                           || Expected HTTP| 200                                            || Expected JSON| `{ "success": true, "sent": N, "failed": 0, "results": [...] }` || Per-result assertions | Each result: `status: "sent"`, `emailSubject` must be RFC 2047 encoded (no raw non-ASCII ΓëÑ 0x80) |**Agent output at this stage:**  ```ΓöÇΓöÇ Step 2: ec_send_all_invitations ΓöÇΓöÇ  Sent: 1, Failed: 0  ec_invite ΓåÆ ranadhir.ghosh@gmail.com: PASS  ec_invite emailSubject RFC 2047 clean: PASS```**Common failure modes:**1. `failed > 0` ΓåÆ Gmail API error. Check OAuth2 token expiry in `GoogleTokens` collection.2. `emailSubject` contains raw non-ASCII ΓåÆ `mimeEncodeHeader()` not called on subject. Fixed in commit `4631dd8` ΓÇö ensure deployed.3. HTTP 500 ΓåÆ HuggingFace LLM timeout (if AI is used in email body). Check `generateEmailResponse()` timeout setting.4. `sent: 0` ΓåÆ No EC members in `AdminRoles` collection (cleanup removed all). Verify Step 0 only revokes the test user, not everyone.**Email verification (manual, not automated):**- Open `ranadhir.ghosh@gmail.com` inbox- Find email from `banfjax@gmail.com` with subject matching invitation template- Check: subject renders without garbled characters- Check: HTML body displays correctly (no base64 visible in body)- Check: onboarding link in body points to `banfjax-hash.github.io/banf/admin-portal.html`---### Stage 3: Admin Signup Direct (Get Setup Token)**Purpose:** The test user initiates account setup by submitting their email. This returns a one-time `setupToken` needed for all subsequent steps.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_signup_direct`         || Headers      | `x-user-email: ""` (no auth ΓÇö public endpoint)|| Body         | `{ "email": "ranadhir.ghosh@gmail.com" }`     || Expected HTTP| 200                                            || Expected JSON| `{ "success": true, "setupToken": "<token>", "role": "ec_member", "ecTitle": "President" }` || Alternate outcome| If account already has a password: HTTP 400, `error` contains "already has a password" ΓåÆ graceful skip to Stage 7 |**Agent behavior on "alreadySet":**  Test skips Stages 4-6 and goes directly to Stage 7 (login test). Logs: `Account already has a password (previous test run). Skipping signup steps.`**Common failure modes:**1. HTTP 400 "email not found" ΓåÆ Stage 1 did not persist to DB. Check `AdminRoles` Wix collection for insert failure.2. `setupToken: null` ΓåÆ Token generation code failed. Check `admin-api.js` token generation logic.3. HTTP 403 ΓåÆ Endpoint has auth check but this must be a public endpoint (no `x-user-email`).---### Stage 4: Set Password**Purpose:** Use the setup token to set a password for the account.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_set_password`          || Headers      | `x-user-email: ""` (public endpoint)          || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "token": "<setupToken>", "password": "TestPass#2026!" }` || Expected HTTP| 200                                            || Expected JSON| `{ "success": true }`                          |**Common failure modes:**1. HTTP 400 "invalid token" ΓåÆ Token expired (check token TTL ΓÇö typically 24h), or token already used.2. HTTP 400 "password too weak" ΓåÆ Password policy change. Current test password meets policy: 12 chars, upper+lower+number+symbol.3. HTTP 500 ΓåÆ bcrypt failure (rare). Check Wix server-side module availability.---### Stage 5: Save Profile**Purpose:** Store the user's name, phone, address, and family members.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_save_profile`          || Headers      | `x-user-email: ""` (public endpoint)          || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "token": "<setupToken>", "firstName": "Ranadhir", "lastName": "Ghosh", "phone": "555-0001", "address": "123 Test St", "city": "Jacksonville", "state": "FL", "zipCode": "32256", "familyMembers": [] }` || Expected HTTP| 200                                            || Expected JSON| `{ "success": true }`                          |**Common failure modes:**1. HTTP 400 after Stage 4 ΓåÆ Token has single-use semantics and was consumed by set_password. Fix: either use separate token, or stage 4+5+6 must reuse the same token if it's multi-use.2. HTTP 500 ΓåÆ Profile collection schema mismatch (field names changed).---### Stage 6: Complete Onboarding**Purpose:** Mark the account as fully onboarded, returning the portal URL.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_onboard_complete`      || Headers      | `x-user-email: ""` (public endpoint)          || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "token": "<setupToken>" }` || Expected HTTP| 200                                            || Expected JSON| `{ "success": true, "portalUrl": "<url containing banfjax-hash.github.io/banf/>" }` |---### Stage 7: Verify Login**Purpose:** Confirm the user can log in with the password set in Stage 4.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_verify_login`          || Headers      | `x-user-email: ""` (public endpoint)          || Body         | `{ "email": "ranadhir.ghosh@gmail.com", "password": "TestPass#2026!" }` || Expected HTTP| 200                                            || Expected JSON| `{ "success": true, "adminRole": "ec_member", "ecTitle": "President" }` |**Common failure modes:**1. `success: false`, `needsOnboarding: true` ΓåÆ Stages 3-6 did not persist. DB write failure.2. `adminRole` wrong ΓåÆ Role stored incorrectly in Stage 1. Check `admin-api.js` role field name.3. HTTP 401/403 ΓåÆ Password hash comparison failed. bcrypt version mismatch.---### Stage 8: EC Congratulations Email**Purpose:** Trigger the congratulations email to the newly onboarded EC member.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/ec_signup_congratulations`   || Headers      | `x-user-email: banfjax@gmail.com`              || Body         | `{ "email": "ranadhir.ghosh@gmail.com" }`     || Expected HTTP| 200                                            || Expected JSON| `{ "success": true, "emailSubject": "<string>" }` || Email assertion | `emailSubject` must be RFC 2047 clean (no raw bytes ΓëÑ 0x80) |**Common failure modes:**1. `success: false` ΓåÆ Gmail API error. OAuth token expired.2. `emailSubject` fails RFC 2047 check ΓåÆ Subject contains em-dash or emoji not encoded via `mimeEncodeHeader()`. Fixed in commit `4631dd8`.3. HTTP 404 ΓåÆ Endpoint not registered in `http-functions.js`.**Email verification (manual):**- Open `ranadhir.ghosh@gmail.com` inbox- Find congratulations email- Verify: subject readable (no ??? or ├óΓé¼" garble)- Verify: HTML body with BANF branding renders correctly---### Verification Stage: Database State Check**Purpose:** Confirm final state in Wix collections matches expectations.| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `GET /_functions/admin_roles`                  || Headers      | `x-user-email: banfjax@gmail.com`              || Expected JSON| `roles` array must contain entry where: `email = "ranadhir.ghosh@gmail.com"`, `role = "ec_member"`, `isActive = true`, `ecTitle = "President"` |---## Cross-Contamination / Isolation TestsThese tests verify EC members are never confused with Stakeholders.### Test 1: EC Email Isolation- Re-add `ranadhir.ghosh@gmail.com` as `ec_member`- Assert `emailSent: false` (EC must NOT get stakeholder welcome email)- Assert `emailSkipped` is present and references `ec_send_all_invitations`- Assert `message` includes `ec_send_all_invitations`### Test 2: URL Correctness- Assert `onboardUrl` contains `banfjax-hash.github.io/banf/`- Assert `onboardUrl` does NOT contain `www.jaxbengali.org`### Test 3: EC Endpoints Reachable- `POST ec_send_all_invitations` ΓåÆ assert status Γëá 404- `POST ec_signup_congratulations` ΓåÆ assert status Γëá 404---## Stakeholder Pipeline (Verification Only)The super_admin (`banfjax@gmail.com`) is a pre-existing account. This pipeline does NOT create ΓÇö only verifies.### Stage S1: Super Admin Login Check| Field        | Value                                          ||--------------|------------------------------------------------|| Endpoint     | `POST /_functions/admin_verify_login`          || Body         | `{ "email": "banfjax@gmail.com", "password": "" }` || Expected response | One of: `success: true` OR `needsOnboarding: true` OR `noPassword: true` |### Stage S2: DB VerificationSame as EC verification ΓÇö confirm `banfjax@gmail.com` exists in `AdminRoles` with `role: "super_admin"`.---## Known Failure Root Causes (The "100 Times Failing" Analysis)Based on full code audit, here are the most likely causes of persistent failures ordered by probability:### #1 ΓÇö MOST LIKELY: TEST_MODE=false Causing Real Emails to Real EC Members**Probability: VERY HIGH**  Every test run of Stage 2 sends real invitations to all EC members in the DB. Members receiving repeat invitations will be confused. This is the user confusion issue. Also makes the test non-idempotent.**Fix required:** Set `TEST_MODE = true` in `ec-onboarding-gate.js` before any test run. Revert to `false` for production use only.**Permanent solution:** Create a dedicated `runIsTestMode()` function that checks a Wix secret (`wix-secrets-backend`) called `BANF_TEST_MODE` instead of hardcoding.### #2 ΓÇö Email Subject Garbling (Fixed in 4631dd8, Needs Verification)**Status:** Code fix deployed, but not verified via physical email delivery.  The `mimeEncodeHeader()` function now encodes em-dash (ΓÇö) and emoji (≡ƒÄë) to RFC 2047 base64 format. However:- The test agent only checks the HTTP response field `emailSubject` ΓÇö not the actual delivered MIME header- Gmail's rendering of `=?UTF-8?B?...?=` in subject line needs real inbox verification**Verification needed:** Send a test congratulations email (via Stage 8) and manually read the received email's subject in the `ranadhir.ghosh@gmail.com` inbox.### #3 ΓÇö Token Reuse Across Stages 3-6**Probability: MEDIUM**  Steps 3 (signup_direct), 4 (set_password), 5 (save_profile), and 6 (onboard_complete) all use the same `setupToken`. If the token is consumed/invalidated after first use, Stage 4 will fail with "invalid token" even though Stage 3 succeeded.**Verification needed:** Run the full pipeline once from scratch (after cleanup) and observe which stage first fails with a token error.### #4 ΓÇö Endpoint Not in http-functions.js**Probability: LOW ΓÇö VERIFIED CLEAR**  All 13 endpoints are confirmed registered in `http-functions.js`.  - `ec_send_all_invitations` and `ec_signup_congratulations` are imported with underscore aliases at lines 283-286 and re-exported as proper async wrapper functions at lines 546-549.  - All other endpoints appear in the direct `export { ... }` block at lines 325+.No action needed on this point.### #5 ΓÇö HuggingFace LLM Timeout in Email Response**Probability: LOW-MEDIUM**  `email-automation.js::generateEmailResponse()` calls HuggingFace `meta-llama/Llama-3.1-8B-Instruct`. This API:- Has cold-start delays (30-60s on free tier)- Returns HTTP 503 when model is loading: `{"error": "Model is currently loading"}`- Returns HTTP 429 on quota exceededCurrent behavior: if LLM fails, the email automation scan fails silently. Errors likely in `banfjax@gmail.com` inbox (forwarded error notifications from Wix).### #6 ΓÇö OAuth2 Token Expiry**Probability: LOW** (refresh token auto-renews, but network errors possible)  `GOOGLE_REFRESH_TOKEN` in `ec-onboarding-gate.js` is hardcoded. If Google revokes it (security event, 6-month inactivity), ALL email sending will fail with HTTP 401 from Gmail API.**Detection:** Gmail sends will return `{ "error": "invalid_grant" }`.  **Fix:** Rotate refresh token via Google OAuth2 playground.---## Email Inbox Error Analysis Plan### banfjax@gmail.com Inbox ΓÇö What to Look ForAfter enabling TEST_MODE and running the pipeline once, scan `banfjax@gmail.com` for:1. **GitHub Actions failure notifications**     Subject: `[banfjax-hash/banf] Run failed: BANF E2E Pipeline Tests`     Body: Contains link to failed run ΓåÆ click ΓåÆ expand "E2E Test Summary" step     **Current status:** NOT failing (all recent runs green)2. **HuggingFace error responses**     Pattern: Emails with subject containing "Error", "Failed", "500", or LLM model name     Body: JSON error from HuggingFace API (`"error": "Model is currently loading"`)     **Action:** Document the error type and retry logic in `agent-orchestrator.js`3. **Wix deployment error emails**     Pattern: Emails from `noreply@wix.com` or `team@wix.com`     Body: Backend function crash stack traces4. **Bounced email delivery failures**     Pattern: Gmail Mailer Daemon ΓÇö `Delivery Status Notification (Failure)`     Body: SMTP reject reason (550 user not found, 421 rate limit, etc.)### Email Scan Endpoint```POST /_functions/email_automation_scanHeaders: x-user-email: banfjax@gmail.comExpected response: { "scanned": N, "queued": M, "errors": [] }```This triggers `runEmailAutomationScan()` in `email-automation.js`, which scans the `InboxMessages` Wix collection (NOT Gmail directly ΓÇö messages must be pre-synced via Gmail API sync).---## Execution Sequence (Approved Plan)### Phase 1 ΓÇö Safety Setup (BEFORE any test)1. [ ] Set `TEST_MODE = true` in `ec-onboarding-gate.js` line 402. [ ] Commit + push ΓåÆ wait for GitHub Actions Pages + E2E both green3. [ ] Verify `GET /deploy_check` returns new version4. [ ] Scan `banfjax@gmail.com` inbox for any existing error emails ΓÇö document them### Phase 2 ΓÇö Endpoint Health Check (No data modification)5. [ ] Run `node banf-pipeline-test-agent.js --admin-only` just for the health check suite6. [ ] Verify all 13 endpoints return Γëá 4047. [ ] Fix any 404s (add missing export to `http-functions.js`), redeploy, repeat### Phase 3 ΓÇö Fresh EC Pipeline (First Real Run)8. [ ] Run `node banf-pipeline-test-agent.js --ec-only` with full logging9. [ ] Capture the exact failure output at each stage10. [ ] After run, manually open `ranadhir.ghosh@gmail.com` and verify any emails received### Phase 4 ΓÇö Email Quality Verification11. [ ] Check invitation email (Stage 2): subject readable, body renders, link works12. [ ] Check congrats email (Stage 8): subject readable, body renders13. [ ] If subject garbled ΓåÆ verify `mimeEncodeHeader()` is called at the point of header construction### Phase 5 ΓÇö Stakeholder + Full Run14. [ ] Run `node banf-pipeline-test-agent.js` (full pipeline including isolation tests)15. [ ] Review `banf-pipeline-test-results.json` for any regressions### Phase 6 ΓÇö Restore to Live Mode16. [ ] Set `TEST_MODE = false` in `ec-onboarding-gate.js`17. [ ] Commit + push18. [ ] Verify GitHub Actions passes19. [ ] DONE ΓÇö production is safe---## Files That Must Not Be Modified During TestingThese files handle real member data ΓÇö any changes require a separate approval:- `banf-wix-linked/src/backend/email-automation.js` (live email auto-responder)- `banf-wix-linked/src/backend/bosonto-email-sender.js` (Bosonto event emails)- Any Wix collection schema changes---## Open Questions Before Execution1. **Is there an existing batch of EC members already in `AdminRoles`?**     If yes, Stage 2 (`ec_send_all_invitations`) with `TEST_MODE=false` has already been sending them repeated invitations on every CI run. We need to confirm this.2. **Has `ec_send_all_invitations` been called by GitHub Actions CI runs?**     Check: does the GitHub Actions workflow run the full pipeline (not `--ec-only`)? If yes, Stage 2 ran on every push. Check `banf-pipeline-test-results.json` artifacts via GitHub UI.3. **Are there any GitHub Actions secrets needed?**     The pipeline test makes no GitHub secret calls ΓÇö it uses hardcoded credentials in the test file. This is a security risk; credentials should be moved to GitHub Secrets.---## Summary of Required Code Changes (Before Execution)| Priority | File | Change | Risk ||----------|------|--------|------|| CRITICAL | `ec-onboarding-gate.js` line 40 | `TEST_MODE = true` for test runs | Must revert to `false` after testing || HIGH | `banf-pipeline-test-agent.js` | Verify `GITHUB_REPO = 'banfjax-hash/banf'` is correct | Already correct || MEDIUM | `http-functions.js` | Verify all 13 endpoints exported | Check before running || LOW | `ec-onboarding-gate.js` | Replace hardcoded `TEST_MODE` with Wix secret | Permanent fix, lower urgency |