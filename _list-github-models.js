const https = require('https');const { execSync } = require('child_process');const token = execSync('gh auth token', { encoding: 'utf8' }).trim();const opts = {  hostname: 'models.github.ai',  path: '/catalog/models',  headers: {    'Accept': 'application/vnd.github+json',    'Authorization': `Bearer ${token}`,    'X-GitHub-Api-Version': '2022-11-28'  }};https.get(opts, res => {  let data = '';  res.on('data', chunk => data += chunk);  res.on('end', () => {    const models = JSON.parse(data);    console.log(`Total models: ${models.length}\n`);    models.forEach(m => {      const inp = (m.supported_input_modalities || []).join(',');      console.log(`${m.id} | ${m.publisher} | tier:${m.rate_limit_tier} | input:${inp}`);    });  });}).on('error', e => console.error(e));