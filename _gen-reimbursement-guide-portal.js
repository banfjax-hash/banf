// One-time script to generate reimbursement guide portal moduleconst fs = require('fs');const html = fs.readFileSync('c:/projects/banf/reimbursement-guide.html', 'utf-8');// Escape for JS template literalconst escaped = html    .replace(/\\/g, '\\\\')    .replace(/`/g, '\\`')    .replace(/\$\{/g, '\\${');const out = `// Auto-generated portal module for reimbursement guide page// Source: reimbursement-guide.htmlexport function getHtml() { return \`${escaped}\`; }`;const outPath = 'c:/projects/banf/banf-wix-linked/src/backend/portals/reimbursement-guide-html.js';fs.writeFileSync(outPath, out);console.log('Created:', outPath, '(' + out.length + ' chars)');